<mujoco model="flexcomp_direct">
  <!-- GATED (mjs.flexcomp_direct). type="direct" supplies inline point/element
       topology; mjs_makeFlex has no point/element surface (build fails "Point
       and element required"). Forced MjsPath must error loudly; Auto must fall
       back to XmlPath. See test_path_diff_gated.py. -->
  <worldbody>
    <geom name="floor" type="plane" size="2 2 0.1"/>
    <flexcomp name="sheet" type="direct" dim="2" radius="0.01" pos="0 0 1"
              point="0 0 0  0.2 0 0  0.2 0.2 0  0 0.2 0"
              element="0 1 2  0 2 3"/>
  </worldbody>
</mujoco>
