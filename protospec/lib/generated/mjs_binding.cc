// Generated by protospec_gen.emit_mjs — do not edit.
#include "mjs_binding.h"

#include <cstddef>

#include "mjs_convert.h"

namespace ps::mjcf {
namespace {

int ToMjt(BiasType v) {
  switch (v) {
    case BiasType::none: return mjBIAS_NONE;
    case BiasType::affine: return mjBIAS_AFFINE;
    case BiasType::muscle: return mjBIAS_MUSCLE;
    case BiasType::dcmotor: return mjBIAS_DCMOTOR;
    case BiasType::user: return mjBIAS_USER;
    default: return 0;
  }
}

int ToMjt(BodySleep v) {
  switch (v) {
    case BodySleep::auto_: return mjSLEEP_AUTO;
    case BodySleep::never: return mjSLEEP_NEVER;
    case BodySleep::allowed: return mjSLEEP_ALLOWED;
    case BodySleep::init: return mjSLEEP_INIT;
    default: return 0;
  }
}

int ToMjt(CamLightMode v) {
  switch (v) {
    case CamLightMode::fixed: return mjCAMLIGHT_FIXED;
    case CamLightMode::track: return mjCAMLIGHT_TRACK;
    case CamLightMode::trackcom: return mjCAMLIGHT_TRACKCOM;
    case CamLightMode::targetbody: return mjCAMLIGHT_TARGETBODY;
    case CamLightMode::targetbodycom: return mjCAMLIGHT_TARGETBODYCOM;
    default: return 0;
  }
}

int ToMjt(CameraProjection v) {
  switch (v) {
    case CameraProjection::perspective: return mjPROJ_PERSPECTIVE;
    case CameraProjection::orthographic: return mjPROJ_ORTHOGRAPHIC;
    default: return 0;
  }
}

int ToMjt(ColorSpace v) {
  switch (v) {
    case ColorSpace::auto_: return mjCOLORSPACE_AUTO;
    case ColorSpace::linear: return mjCOLORSPACE_LINEAR;
    case ColorSpace::sRGB: return mjCOLORSPACE_SRGB;
    default: return 0;
  }
}

int ToMjt(DataType v) {
  switch (v) {
    case DataType::real: return mjDATATYPE_REAL;
    case DataType::positive: return mjDATATYPE_POSITIVE;
    case DataType::axis: return mjDATATYPE_AXIS;
    case DataType::quaternion: return mjDATATYPE_QUATERNION;
    default: return 0;
  }
}

int ToMjt(DynType v) {
  switch (v) {
    case DynType::none: return mjDYN_NONE;
    case DynType::integrator: return mjDYN_INTEGRATOR;
    case DynType::filter: return mjDYN_FILTER;
    case DynType::filterexact: return mjDYN_FILTEREXACT;
    case DynType::muscle: return mjDYN_MUSCLE;
    case DynType::dcmotor: return mjDYN_DCMOTOR;
    case DynType::user: return mjDYN_USER;
    default: return 0;
  }
}

int ToMjt(GainType v) {
  switch (v) {
    case GainType::fixed: return mjGAIN_FIXED;
    case GainType::affine: return mjGAIN_AFFINE;
    case GainType::muscle: return mjGAIN_MUSCLE;
    case GainType::dcmotor: return mjGAIN_DCMOTOR;
    case GainType::user: return mjGAIN_USER;
    default: return 0;
  }
}

int ToMjt(GeomType v) {
  switch (v) {
    case GeomType::plane: return mjGEOM_PLANE;
    case GeomType::hfield: return mjGEOM_HFIELD;
    case GeomType::sphere: return mjGEOM_SPHERE;
    case GeomType::capsule: return mjGEOM_CAPSULE;
    case GeomType::ellipsoid: return mjGEOM_ELLIPSOID;
    case GeomType::cylinder: return mjGEOM_CYLINDER;
    case GeomType::box: return mjGEOM_BOX;
    case GeomType::mesh: return mjGEOM_MESH;
    case GeomType::sdf: return mjGEOM_SDF;
    default: return 0;
  }
}

int ToMjt(InertiaFromGeom v) {
  switch (v) {
    case InertiaFromGeom::false_: return mjINERTIAFROMGEOM_FALSE;
    case InertiaFromGeom::true_: return mjINERTIAFROMGEOM_TRUE;
    case InertiaFromGeom::auto_: return mjINERTIAFROMGEOM_AUTO;
    default: return 0;
  }
}

int ToMjt(InterpType v) {
  switch (v) {
    case InterpType::zoh: return 0;
    case InterpType::linear: return 1;
    case InterpType::cubic: return 2;
    default: return 0;
  }
}

int ToMjt(JointType v) {
  switch (v) {
    case JointType::free: return mjJNT_FREE;
    case JointType::ball: return mjJNT_BALL;
    case JointType::slide: return mjJNT_SLIDE;
    case JointType::hinge: return mjJNT_HINGE;
    default: return 0;
  }
}

int ToMjt(LightType v) {
  switch (v) {
    case LightType::spot: return mjLIGHT_SPOT;
    case LightType::directional: return mjLIGHT_DIRECTIONAL;
    case LightType::point: return mjLIGHT_POINT;
    case LightType::image: return mjLIGHT_IMAGE;
    default: return 0;
  }
}

int ToMjt(MeshInertia v) {
  switch (v) {
    case MeshInertia::convex: return mjMESH_INERTIA_CONVEX;
    case MeshInertia::exact: return mjMESH_INERTIA_EXACT;
    case MeshInertia::legacy: return mjMESH_INERTIA_LEGACY;
    case MeshInertia::shell: return mjMESH_INERTIA_SHELL;
    default: return 0;
  }
}

int ToMjt(NeedStage v) {
  switch (v) {
    case NeedStage::none: return mjSTAGE_NONE;
    case NeedStage::pos: return mjSTAGE_POS;
    case NeedStage::vel: return mjSTAGE_VEL;
    case NeedStage::acc: return mjSTAGE_ACC;
    default: return 0;
  }
}

int ToMjt(TextureMark v) {
  switch (v) {
    case TextureMark::none: return mjMARK_NONE;
    case TextureMark::edge: return mjMARK_EDGE;
    case TextureMark::cross: return mjMARK_CROSS;
    case TextureMark::random: return mjMARK_RANDOM;
    default: return 0;
  }
}

int ToMjt(TextureType v) {
  switch (v) {
    case TextureType::twod: return mjTEXTURE_2D;
    case TextureType::cube: return mjTEXTURE_CUBE;
    case TextureType::skybox: return mjTEXTURE_SKYBOX;
    default: return 0;
  }
}

int ToMjt(TriState v) {
  switch (v) {
    case TriState::false_: return mjLIMITED_FALSE;
    case TriState::true_: return mjLIMITED_TRUE;
    case TriState::auto_: return mjLIMITED_AUTO;
    default: return 0;
  }
}

}  // namespace

void ApplyMjs(const Accelerometer& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const ActuatorGeneral& e, mjsActuator* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.group.has_value()) { out->group = (*e.group); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.ctrllimited.has_value()) { out->ctrllimited = static_cast<int>(ToMjt((*e.ctrllimited))); }
  if (e.forcelimited.has_value()) { out->forcelimited = static_cast<int>(ToMjt((*e.forcelimited))); }
  if (e.actlimited.has_value()) { out->actlimited = static_cast<int>(ToMjt((*e.actlimited))); }
  if (e.ctrlrange.has_value()) { for (int i = 0; i < 2; ++i) out->ctrlrange[i] = (*e.ctrlrange)[i]; }
  if (e.forcerange.has_value()) { for (int i = 0; i < 2; ++i) out->forcerange[i] = (*e.forcerange)[i]; }
  if (e.actrange.has_value()) { for (int i = 0; i < 2; ++i) out->actrange[i] = (*e.actrange)[i]; }
  if (e.lengthrange.has_value()) { for (int i = 0; i < 2; ++i) out->lengthrange[i] = (*e.lengthrange)[i]; }
  if (e.gear.has_value()) { for (std::size_t i = 0; i < (*e.gear).size(); ++i) out->gear[i] = (*e.gear)[i]; }
  if (e.damping.has_value()) { for (std::size_t i = 0; i < (*e.damping).size(); ++i) out->damping[i] = (*e.damping)[i]; }
  if (e.armature.has_value()) { out->armature = (*e.armature); }
  if (e.cranklength.has_value()) { out->cranklength = (*e.cranklength); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  if (e.slidersite.has_value()) { mjs_setString(out->slidersite, (*e.slidersite).name.c_str()); }
  if (e.refsite.has_value()) { mjs_setString(out->refsite, (*e.refsite).name.c_str()); }
  if (e.actdim.has_value()) { out->actdim = (*e.actdim); }
  if (e.dyntype.has_value()) { out->dyntype = static_cast<mjtDyn>(ToMjt((*e.dyntype))); }
  if (e.gaintype.has_value()) { out->gaintype = static_cast<mjtGain>(ToMjt((*e.gaintype))); }
  if (e.biastype.has_value()) { out->biastype = static_cast<mjtBias>(ToMjt((*e.biastype))); }
  if (e.dynprm.has_value()) { for (std::size_t i = 0; i < (*e.dynprm).size(); ++i) out->dynprm[i] = (*e.dynprm)[i]; }
  if (e.gainprm.has_value()) { for (std::size_t i = 0; i < (*e.gainprm).size(); ++i) out->gainprm[i] = (*e.gainprm)[i]; }
  if (e.biasprm.has_value()) { for (std::size_t i = 0; i < (*e.biasprm).size(); ++i) out->biasprm[i] = (*e.biasprm)[i]; }
  if (e.actearly.has_value()) { out->actearly = (*e.actearly); }
  (void)e;
  (void)out;
}

void ApplyMjs(const ActuatorPlugin& e, mjsActuator* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.group.has_value()) { out->group = (*e.group); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.ctrllimited.has_value()) { out->ctrllimited = static_cast<int>(ToMjt((*e.ctrllimited))); }
  if (e.forcelimited.has_value()) { out->forcelimited = static_cast<int>(ToMjt((*e.forcelimited))); }
  if (e.actlimited.has_value()) { out->actlimited = static_cast<int>(ToMjt((*e.actlimited))); }
  if (e.ctrlrange.has_value()) { for (int i = 0; i < 2; ++i) out->ctrlrange[i] = (*e.ctrlrange)[i]; }
  if (e.forcerange.has_value()) { for (int i = 0; i < 2; ++i) out->forcerange[i] = (*e.forcerange)[i]; }
  if (e.actrange.has_value()) { for (int i = 0; i < 2; ++i) out->actrange[i] = (*e.actrange)[i]; }
  if (e.lengthrange.has_value()) { for (int i = 0; i < 2; ++i) out->lengthrange[i] = (*e.lengthrange)[i]; }
  if (e.gear.has_value()) { for (std::size_t i = 0; i < (*e.gear).size(); ++i) out->gear[i] = (*e.gear)[i]; }
  if (e.damping.has_value()) { for (std::size_t i = 0; i < (*e.damping).size(); ++i) out->damping[i] = (*e.damping)[i]; }
  if (e.armature.has_value()) { out->armature = (*e.armature); }
  if (e.cranklength.has_value()) { out->cranklength = (*e.cranklength); }
  if (e.actdim.has_value()) { out->actdim = (*e.actdim); }
  if (e.dyntype.has_value()) { out->dyntype = static_cast<mjtDyn>(ToMjt((*e.dyntype))); }
  if (e.dynprm.has_value()) { for (std::size_t i = 0; i < (*e.dynprm).size(); ++i) out->dynprm[i] = (*e.dynprm)[i]; }
  if (e.slidersite.has_value()) { mjs_setString(out->slidersite, (*e.slidersite).name.c_str()); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  if (e.actearly.has_value()) { out->actearly = (*e.actearly); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Actuatorfrc& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Actuatorpos& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Actuatorvel& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Adhesion& e, mjsActuator* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.group.has_value()) { out->group = (*e.group); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.forcelimited.has_value()) { out->forcelimited = static_cast<int>(ToMjt((*e.forcelimited))); }
  if (e.ctrlrange.has_value()) { for (int i = 0; i < 2; ++i) out->ctrlrange[i] = (*e.ctrlrange)[i]; }
  if (e.forcerange.has_value()) { for (int i = 0; i < 2; ++i) out->forcerange[i] = (*e.forcerange)[i]; }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Ballangvel& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Ballquat& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Body& e, mjsBody* out) {
  if (e.pos.has_value()) { for (int i = 0; i < 3; ++i) out->pos[i] = (*e.pos)[i]; }
  if (e.quat.has_value()) { for (int i = 0; i < 4; ++i) out->quat[i] = (*e.quat)[i]; }
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.childclass.has_value()) { mjs_setString(out->childclass, (*e.childclass).name.c_str()); }
  if (e.mocap.has_value()) { out->mocap = (*e.mocap); }
  if (e.gravcomp.has_value()) { out->gravcomp = (*e.gravcomp); }
  if (e.sleep.has_value()) { out->sleep = static_cast<mjtSleepPolicy>(ToMjt((*e.sleep))); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Camera& e, mjsCamera* out) {
  if (e.pos.has_value()) { for (int i = 0; i < 3; ++i) out->pos[i] = (*e.pos)[i]; }
  if (e.quat.has_value()) { for (int i = 0; i < 4; ++i) out->quat[i] = (*e.quat)[i]; }
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.projection.has_value()) { out->proj = static_cast<mjtProjection>(ToMjt((*e.projection))); }
  if (e.ipd.has_value()) { out->ipd = (*e.ipd); }
  if (e.resolution.has_value()) { for (int i = 0; i < 2; ++i) out->resolution[i] = (*e.resolution)[i]; }
  if (e.mode.has_value()) { out->mode = static_cast<mjtCamLight>(ToMjt((*e.mode))); }
  if (e.target.has_value()) { mjs_setString(out->targetbody, (*e.target).name.c_str()); }
  if (e.focalpixel.has_value()) { for (int i = 0; i < 2; ++i) out->focal_pixel[i] = (*e.focalpixel)[i]; }
  if (e.principal.has_value()) { for (int i = 0; i < 2; ++i) out->principal_length[i] = (*e.principal)[i]; }
  if (e.principalpixel.has_value()) { for (int i = 0; i < 2; ++i) out->principal_pixel[i] = (*e.principalpixel)[i]; }
  if (e.sensorsize.has_value()) { for (int i = 0; i < 2; ++i) out->sensor_size[i] = (*e.sensorsize)[i]; }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  if (e.fovy.has_value()) { out->fovy = (*e.fovy); }
  if (e.focal.has_value()) { for (int i = 0; i < 2; ++i) out->focal_length[i] = (*e.focal)[i]; }
  (void)e;
  (void)out;
}

void ApplyMjs(const Camprojection& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Clock& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Compiler& e, mjsCompiler* out) {
  if (e.autolimits.has_value()) { out->autolimits = (*e.autolimits); }
  if (e.boundmass.has_value()) { out->boundmass = (*e.boundmass); }
  if (e.boundinertia.has_value()) { out->boundinertia = (*e.boundinertia); }
  if (e.settotalmass.has_value()) { out->settotalmass = (*e.settotalmass); }
  if (e.balanceinertia.has_value()) { out->balanceinertia = (*e.balanceinertia); }
  if (e.fitaabb.has_value()) { out->fitaabb = (*e.fitaabb); }
  if (e.meshdir.has_value()) { mjs_setString(out->meshdir, (*e.meshdir).c_str()); }
  if (e.texturedir.has_value()) { mjs_setString(out->texturedir, (*e.texturedir).c_str()); }
  if (e.discardvisual.has_value()) { out->discardvisual = (*e.discardvisual); }
  if (e.usethread.has_value()) { out->usethread = (*e.usethread); }
  if (e.fusestatic.has_value()) { out->fusestatic = (*e.fusestatic); }
  if (e.inertiafromgeom.has_value()) { out->inertiafromgeom = static_cast<int>(ToMjt((*e.inertiafromgeom))); }
  if (e.inertiagrouprange.has_value()) { for (int i = 0; i < 2; ++i) out->inertiagrouprange[i] = (*e.inertiagrouprange)[i]; }
  if (e.saveinertial.has_value()) { out->saveinertial = (*e.saveinertial); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Connect& e, mjsEquality* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.active.has_value()) { out->active = (*e.active); }
  if (e.solref.has_value()) { for (std::size_t i = 0; i < (*e.solref).size(); ++i) out->solref[i] = (*e.solref)[i]; }
  if (e.solimp.has_value()) { for (std::size_t i = 0; i < (*e.solimp).size(); ++i) out->solimp[i] = (*e.solimp)[i]; }
  (void)e;
  (void)out;
}

void ApplyMjs(const Cylinder& e, mjsActuator* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.group.has_value()) { out->group = (*e.group); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.ctrllimited.has_value()) { out->ctrllimited = static_cast<int>(ToMjt((*e.ctrllimited))); }
  if (e.forcelimited.has_value()) { out->forcelimited = static_cast<int>(ToMjt((*e.forcelimited))); }
  if (e.ctrlrange.has_value()) { for (int i = 0; i < 2; ++i) out->ctrlrange[i] = (*e.ctrlrange)[i]; }
  if (e.forcerange.has_value()) { for (int i = 0; i < 2; ++i) out->forcerange[i] = (*e.forcerange)[i]; }
  if (e.lengthrange.has_value()) { for (int i = 0; i < 2; ++i) out->lengthrange[i] = (*e.lengthrange)[i]; }
  if (e.gear.has_value()) { for (std::size_t i = 0; i < (*e.gear).size(); ++i) out->gear[i] = (*e.gear)[i]; }
  if (e.damping.has_value()) { for (std::size_t i = 0; i < (*e.damping).size(); ++i) out->damping[i] = (*e.damping)[i]; }
  if (e.armature.has_value()) { out->armature = (*e.armature); }
  if (e.cranklength.has_value()) { out->cranklength = (*e.cranklength); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  if (e.slidersite.has_value()) { mjs_setString(out->slidersite, (*e.slidersite).name.c_str()); }
  if (e.refsite.has_value()) { mjs_setString(out->refsite, (*e.refsite).name.c_str()); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Damper& e, mjsActuator* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.group.has_value()) { out->group = (*e.group); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.forcelimited.has_value()) { out->forcelimited = static_cast<int>(ToMjt((*e.forcelimited))); }
  if (e.ctrlrange.has_value()) { for (int i = 0; i < 2; ++i) out->ctrlrange[i] = (*e.ctrlrange)[i]; }
  if (e.forcerange.has_value()) { for (int i = 0; i < 2; ++i) out->forcerange[i] = (*e.forcerange)[i]; }
  if (e.lengthrange.has_value()) { for (int i = 0; i < 2; ++i) out->lengthrange[i] = (*e.lengthrange)[i]; }
  if (e.gear.has_value()) { for (std::size_t i = 0; i < (*e.gear).size(); ++i) out->gear[i] = (*e.gear)[i]; }
  if (e.damping.has_value()) { for (std::size_t i = 0; i < (*e.damping).size(); ++i) out->damping[i] = (*e.damping)[i]; }
  if (e.armature.has_value()) { out->armature = (*e.armature); }
  if (e.cranklength.has_value()) { out->cranklength = (*e.cranklength); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  if (e.slidersite.has_value()) { mjs_setString(out->slidersite, (*e.slidersite).name.c_str()); }
  if (e.refsite.has_value()) { mjs_setString(out->refsite, (*e.refsite).name.c_str()); }
  (void)e;
  (void)out;
}

void ApplyMjs(const DcMotor& e, mjsActuator* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.group.has_value()) { out->group = (*e.group); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.ctrllimited.has_value()) { out->ctrllimited = static_cast<int>(ToMjt((*e.ctrllimited))); }
  if (e.ctrlrange.has_value()) { for (int i = 0; i < 2; ++i) out->ctrlrange[i] = (*e.ctrlrange)[i]; }
  if (e.lengthrange.has_value()) { for (int i = 0; i < 2; ++i) out->lengthrange[i] = (*e.lengthrange)[i]; }
  if (e.gear.has_value()) { for (std::size_t i = 0; i < (*e.gear).size(); ++i) out->gear[i] = (*e.gear)[i]; }
  if (e.damping.has_value()) { for (std::size_t i = 0; i < (*e.damping).size(); ++i) out->damping[i] = (*e.damping)[i]; }
  if (e.armature.has_value()) { out->armature = (*e.armature); }
  if (e.cranklength.has_value()) { out->cranklength = (*e.cranklength); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  if (e.slidersite.has_value()) { mjs_setString(out->slidersite, (*e.slidersite).name.c_str()); }
  if (e.refsite.has_value()) { mjs_setString(out->refsite, (*e.refsite).name.c_str()); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Distance& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const EKinetic& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const EPotential& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const EqualityDefault& e, mjsEquality* out) {
  if (e.active.has_value()) { out->active = (*e.active); }
  if (e.solref.has_value()) { for (std::size_t i = 0; i < (*e.solref).size(); ++i) out->solref[i] = (*e.solref)[i]; }
  if (e.solimp.has_value()) { for (std::size_t i = 0; i < (*e.solimp).size(); ++i) out->solimp[i] = (*e.solimp)[i]; }
  (void)e;
  (void)out;
}

void ApplyMjs(const EqualityFlex& e, mjsEquality* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.active.has_value()) { out->active = (*e.active); }
  if (e.solref.has_value()) { for (std::size_t i = 0; i < (*e.solref).size(); ++i) out->solref[i] = (*e.solref)[i]; }
  if (e.solimp.has_value()) { for (std::size_t i = 0; i < (*e.solimp).size(); ++i) out->solimp[i] = (*e.solimp)[i]; }
  (void)e;
  (void)out;
}

void ApplyMjs(const EqualityJoint& e, mjsEquality* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.active.has_value()) { out->active = (*e.active); }
  if (e.solref.has_value()) { for (std::size_t i = 0; i < (*e.solref).size(); ++i) out->solref[i] = (*e.solref)[i]; }
  if (e.solimp.has_value()) { for (std::size_t i = 0; i < (*e.solimp).size(); ++i) out->solimp[i] = (*e.solimp)[i]; }
  (void)e;
  (void)out;
}

void ApplyMjs(const EqualityTendon& e, mjsEquality* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.active.has_value()) { out->active = (*e.active); }
  if (e.solref.has_value()) { for (std::size_t i = 0; i < (*e.solref).size(); ++i) out->solref[i] = (*e.solref)[i]; }
  if (e.solimp.has_value()) { for (std::size_t i = 0; i < (*e.solimp).size(); ++i) out->solimp[i] = (*e.solimp)[i]; }
  (void)e;
  (void)out;
}

void ApplyMjs(const Exclude& e, mjsExclude* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.body1.has_value()) { mjs_setString(out->bodyname1, (*e.body1).name.c_str()); }
  if (e.body2.has_value()) { mjs_setString(out->bodyname2, (*e.body2).name.c_str()); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Fixed& e, mjsTendon* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.group.has_value()) { out->group = (*e.group); }
  if (e.limited.has_value()) { out->limited = static_cast<int>(ToMjt((*e.limited))); }
  if (e.actuatorfrclimited.has_value()) { out->actfrclimited = static_cast<int>(ToMjt((*e.actuatorfrclimited))); }
  if (e.range.has_value()) { for (int i = 0; i < 2; ++i) out->range[i] = (*e.range)[i]; }
  if (e.actuatorfrcrange.has_value()) { for (int i = 0; i < 2; ++i) out->actfrcrange[i] = (*e.actuatorfrcrange)[i]; }
  if (e.solreflimit.has_value()) { for (std::size_t i = 0; i < (*e.solreflimit).size(); ++i) out->solref_limit[i] = (*e.solreflimit)[i]; }
  if (e.solimplimit.has_value()) { for (std::size_t i = 0; i < (*e.solimplimit).size(); ++i) out->solimp_limit[i] = (*e.solimplimit)[i]; }
  if (e.solreffriction.has_value()) { for (std::size_t i = 0; i < (*e.solreffriction).size(); ++i) out->solref_friction[i] = (*e.solreffriction)[i]; }
  if (e.solimpfriction.has_value()) { for (std::size_t i = 0; i < (*e.solimpfriction).size(); ++i) out->solimp_friction[i] = (*e.solimpfriction)[i]; }
  if (e.frictionloss.has_value()) { out->frictionloss = (*e.frictionloss); }
  if (e.springlength.has_value()) { for (int i = 0; i < 2; ++i) out->springlength[i] = (*e.springlength)[i]; }
  if (e.margin.has_value()) { out->margin = (*e.margin); }
  if (e.stiffness.has_value()) { for (std::size_t i = 0; i < (*e.stiffness).size(); ++i) out->stiffness[i] = (*e.stiffness)[i]; }
  if (e.damping.has_value()) { for (std::size_t i = 0; i < (*e.damping).size(); ++i) out->damping[i] = (*e.damping)[i]; }
  if (e.armature.has_value()) { out->armature = (*e.armature); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Flex& e, mjsFlex* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.group.has_value()) { out->group = (*e.group); }
  if (e.dim.has_value()) { out->dim = (*e.dim); }
  if (e.radius.has_value()) { out->radius = (*e.radius); }
  if (e.material.has_value()) { mjs_setString(out->material, (*e.material).name.c_str()); }
  if (e.rgba.has_value()) { for (int i = 0; i < 4; ++i) out->rgba[i] = (*e.rgba)[i]; }
  if (e.flatskin.has_value()) { out->flatskin = (*e.flatskin); }
  if (e.body.has_value()) { ps::mjcf::compile::SetRefVec(out->vertbody, (*e.body)); }
  if (e.vertex.has_value()) { ps::mjcf::compile::SetDouble(out->vert, (*e.vertex)); }
  if (e.element.has_value()) { ps::mjcf::compile::SetInt(out->elem, (*e.element)); }
  if (e.texcoord.has_value()) { ps::mjcf::compile::SetFloat(out->texcoord, (*e.texcoord)); }
  if (e.elemtexcoord.has_value()) { ps::mjcf::compile::SetInt(out->elemtexcoord, (*e.elemtexcoord)); }
  if (e.node.has_value()) { ps::mjcf::compile::SetRefVec(out->nodebody, (*e.node)); }
  if (e.cellcount.has_value()) { for (int i = 0; i < 3; ++i) out->cellcount[i] = (*e.cellcount)[i]; }
  (void)e;
  (void)out;
}

void ApplyMjs(const Flexstrain& e, mjsEquality* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.active.has_value()) { out->active = (*e.active); }
  if (e.solref.has_value()) { for (std::size_t i = 0; i < (*e.solref).size(); ++i) out->solref[i] = (*e.solref)[i]; }
  if (e.solimp.has_value()) { for (std::size_t i = 0; i < (*e.solimp).size(); ++i) out->solimp[i] = (*e.solimp)[i]; }
  (void)e;
  (void)out;
}

void ApplyMjs(const Flexvert& e, mjsEquality* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.active.has_value()) { out->active = (*e.active); }
  if (e.solref.has_value()) { for (std::size_t i = 0; i < (*e.solref).size(); ++i) out->solref[i] = (*e.solref)[i]; }
  if (e.solimp.has_value()) { for (std::size_t i = 0; i < (*e.solimp).size(); ++i) out->solimp[i] = (*e.solimp)[i]; }
  (void)e;
  (void)out;
}

void ApplyMjs(const Force& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Frame& e, mjsFrame* out) {
  if (e.pos.has_value()) { for (int i = 0; i < 3; ++i) out->pos[i] = (*e.pos)[i]; }
  if (e.quat.has_value()) { for (int i = 0; i < 4; ++i) out->quat[i] = (*e.quat)[i]; }
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.dclass.has_value()) { mjs_setString(out->childclass, (*e.dclass).name.c_str()); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Frameangacc& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.objname.has_value()) { mjs_setString(out->objname, (*e.objname).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Frameangvel& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.objname.has_value()) { mjs_setString(out->objname, (*e.objname).c_str()); }
  if (e.refname.has_value()) { mjs_setString(out->refname, (*e.refname).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Framelinacc& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.objname.has_value()) { mjs_setString(out->objname, (*e.objname).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Framelinvel& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.objname.has_value()) { mjs_setString(out->objname, (*e.objname).c_str()); }
  if (e.refname.has_value()) { mjs_setString(out->refname, (*e.refname).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Framepos& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.objname.has_value()) { mjs_setString(out->objname, (*e.objname).c_str()); }
  if (e.refname.has_value()) { mjs_setString(out->refname, (*e.refname).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Framequat& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.objname.has_value()) { mjs_setString(out->objname, (*e.objname).c_str()); }
  if (e.refname.has_value()) { mjs_setString(out->refname, (*e.refname).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Framexaxis& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.objname.has_value()) { mjs_setString(out->objname, (*e.objname).c_str()); }
  if (e.refname.has_value()) { mjs_setString(out->refname, (*e.refname).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Frameyaxis& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.objname.has_value()) { mjs_setString(out->objname, (*e.objname).c_str()); }
  if (e.refname.has_value()) { mjs_setString(out->refname, (*e.refname).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Framezaxis& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.objname.has_value()) { mjs_setString(out->objname, (*e.objname).c_str()); }
  if (e.refname.has_value()) { mjs_setString(out->refname, (*e.refname).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const FreeJoint& e, mjsJoint* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.group.has_value()) { out->group = (*e.group); }
  if (e.align.has_value()) { out->align = static_cast<int>(ToMjt((*e.align))); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Fromto& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Geom& e, mjsGeom* out) {
  if (e.pos.has_value()) { for (int i = 0; i < 3; ++i) out->pos[i] = (*e.pos)[i]; }
  if (e.quat.has_value()) { for (int i = 0; i < 4; ++i) out->quat[i] = (*e.quat)[i]; }
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.type.has_value()) { out->type = static_cast<mjtGeom>(ToMjt((*e.type))); }
  if (e.contype.has_value()) { out->contype = (*e.contype); }
  if (e.conaffinity.has_value()) { out->conaffinity = (*e.conaffinity); }
  if (e.condim.has_value()) { out->condim = (*e.condim); }
  if (e.group.has_value()) { out->group = (*e.group); }
  if (e.priority.has_value()) { out->priority = (*e.priority); }
  if (e.size.has_value()) { for (std::size_t i = 0; i < (*e.size).size(); ++i) out->size[i] = (*e.size)[i]; }
  if (e.material.has_value()) { mjs_setString(out->material, (*e.material).name.c_str()); }
  if (e.friction.has_value()) { for (std::size_t i = 0; i < (*e.friction).size(); ++i) out->friction[i] = (*e.friction)[i]; }
  if (e.mass.has_value()) { out->mass = (*e.mass); }
  if (e.density.has_value()) { out->density = (*e.density); }
  if (e.solmix.has_value()) { out->solmix = (*e.solmix); }
  if (e.solref.has_value()) { for (std::size_t i = 0; i < (*e.solref).size(); ++i) out->solref[i] = (*e.solref)[i]; }
  if (e.solimp.has_value()) { for (std::size_t i = 0; i < (*e.solimp).size(); ++i) out->solimp[i] = (*e.solimp)[i]; }
  if (e.margin.has_value()) { out->margin = (*e.margin); }
  if (e.gap.has_value()) { out->gap = (*e.gap); }
  if (e.hfield.has_value()) { mjs_setString(out->hfieldname, (*e.hfield).name.c_str()); }
  if (e.mesh.has_value()) { mjs_setString(out->meshname, (*e.mesh).name.c_str()); }
  if (e.fitscale.has_value()) { out->fitscale = (*e.fitscale); }
  if (e.rgba.has_value()) { for (int i = 0; i < 4; ++i) out->rgba[i] = (*e.rgba)[i]; }
  if (e.fluidcoef.has_value()) { for (std::size_t i = 0; i < (*e.fluidcoef).size(); ++i) out->fluid_coefs[i] = (*e.fluidcoef)[i]; }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  if (e.shape.has_value()) { ps::mjcf::compile::ApplyShape((*e.shape), out); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Gyro& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Hfield& e, mjsHField* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.content_type.has_value()) { mjs_setString(out->content_type, (*e.content_type).c_str()); }
  if (e.file.has_value()) { mjs_setString(out->file, (*e.file).c_str()); }
  if (e.nrow.has_value()) { out->nrow = (*e.nrow); }
  if (e.ncol.has_value()) { out->ncol = (*e.ncol); }
  if (e.size.has_value()) { for (int i = 0; i < 4; ++i) out->size[i] = (*e.size)[i]; }
  if (e.elevation.has_value()) { ps::mjcf::compile::SetFloat(out->userdata, (*e.elevation)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Inertial& e, mjsBody* out) {
  if (e.pos.has_value()) { for (int i = 0; i < 3; ++i) out->pos[i] = (*e.pos)[i]; }
  if (e.mass.has_value()) { out->mass = (*e.mass); }
  if (e.iquat.has_value()) { for (int i = 0; i < 4; ++i) out->iquat[i] = (*e.iquat)[i]; }
  if (e.diaginertia.has_value()) { for (int i = 0; i < 3; ++i) out->inertia[i] = (*e.diaginertia)[i]; }
  (void)e;
  (void)out;
}

void ApplyMjs(const Insidesite& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.objname.has_value()) { mjs_setString(out->objname, (*e.objname).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const IntVelocity& e, mjsActuator* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.group.has_value()) { out->group = (*e.group); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.ctrllimited.has_value()) { out->ctrllimited = static_cast<int>(ToMjt((*e.ctrllimited))); }
  if (e.forcelimited.has_value()) { out->forcelimited = static_cast<int>(ToMjt((*e.forcelimited))); }
  if (e.ctrlrange.has_value()) { for (int i = 0; i < 2; ++i) out->ctrlrange[i] = (*e.ctrlrange)[i]; }
  if (e.forcerange.has_value()) { for (int i = 0; i < 2; ++i) out->forcerange[i] = (*e.forcerange)[i]; }
  if (e.actrange.has_value()) { for (int i = 0; i < 2; ++i) out->actrange[i] = (*e.actrange)[i]; }
  if (e.lengthrange.has_value()) { for (int i = 0; i < 2; ++i) out->lengthrange[i] = (*e.lengthrange)[i]; }
  if (e.gear.has_value()) { for (std::size_t i = 0; i < (*e.gear).size(); ++i) out->gear[i] = (*e.gear)[i]; }
  if (e.damping.has_value()) { for (std::size_t i = 0; i < (*e.damping).size(); ++i) out->damping[i] = (*e.damping)[i]; }
  if (e.armature.has_value()) { out->armature = (*e.armature); }
  if (e.cranklength.has_value()) { out->cranklength = (*e.cranklength); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  if (e.slidersite.has_value()) { mjs_setString(out->slidersite, (*e.slidersite).name.c_str()); }
  if (e.refsite.has_value()) { mjs_setString(out->refsite, (*e.refsite).name.c_str()); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Joint& e, mjsJoint* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.type.has_value()) { out->type = static_cast<mjtJoint>(ToMjt((*e.type))); }
  if (e.group.has_value()) { out->group = (*e.group); }
  if (e.pos.has_value()) { for (int i = 0; i < 3; ++i) out->pos[i] = (*e.pos)[i]; }
  if (e.axis.has_value()) { for (int i = 0; i < 3; ++i) out->axis[i] = (*e.axis)[i]; }
  if (e.springdamper.has_value()) { for (int i = 0; i < 2; ++i) out->springdamper[i] = (*e.springdamper)[i]; }
  if (e.limited.has_value()) { out->limited = static_cast<int>(ToMjt((*e.limited))); }
  if (e.actuatorfrclimited.has_value()) { out->actfrclimited = static_cast<int>(ToMjt((*e.actuatorfrclimited))); }
  if (e.solreflimit.has_value()) { for (std::size_t i = 0; i < (*e.solreflimit).size(); ++i) out->solref_limit[i] = (*e.solreflimit)[i]; }
  if (e.solimplimit.has_value()) { for (std::size_t i = 0; i < (*e.solimplimit).size(); ++i) out->solimp_limit[i] = (*e.solimplimit)[i]; }
  if (e.solreffriction.has_value()) { for (std::size_t i = 0; i < (*e.solreffriction).size(); ++i) out->solref_friction[i] = (*e.solreffriction)[i]; }
  if (e.solimpfriction.has_value()) { for (std::size_t i = 0; i < (*e.solimpfriction).size(); ++i) out->solimp_friction[i] = (*e.solimpfriction)[i]; }
  if (e.stiffness.has_value()) { for (std::size_t i = 0; i < (*e.stiffness).size(); ++i) out->stiffness[i] = (*e.stiffness)[i]; }
  if (e.range.has_value()) { for (int i = 0; i < 2; ++i) out->range[i] = (*e.range)[i]; }
  if (e.actuatorfrcrange.has_value()) { for (int i = 0; i < 2; ++i) out->actfrcrange[i] = (*e.actuatorfrcrange)[i]; }
  if (e.actuatorgravcomp.has_value()) { out->actgravcomp = (*e.actuatorgravcomp); }
  if (e.margin.has_value()) { out->margin = (*e.margin); }
  if (e.ref.has_value()) { out->ref = (*e.ref); }
  if (e.springref.has_value()) { out->springref = (*e.springref); }
  if (e.armature.has_value()) { out->armature = (*e.armature); }
  if (e.damping.has_value()) { for (std::size_t i = 0; i < (*e.damping).size(); ++i) out->damping[i] = (*e.damping)[i]; }
  if (e.frictionloss.has_value()) { out->frictionloss = (*e.frictionloss); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Jointactuatorfrc& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Jointlimitfrc& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Jointlimitpos& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Jointlimitvel& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Jointpos& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Jointvel& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Key& e, mjsKey* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.time.has_value()) { out->time = (*e.time); }
  if (e.qpos.has_value()) { ps::mjcf::compile::SetDouble(out->qpos, (*e.qpos)); }
  if (e.qvel.has_value()) { ps::mjcf::compile::SetDouble(out->qvel, (*e.qvel)); }
  if (e.act.has_value()) { ps::mjcf::compile::SetDouble(out->act, (*e.act)); }
  if (e.mpos.has_value()) { ps::mjcf::compile::SetDouble(out->mpos, (*e.mpos)); }
  if (e.mquat.has_value()) { ps::mjcf::compile::SetDouble(out->mquat, (*e.mquat)); }
  if (e.ctrl.has_value()) { ps::mjcf::compile::SetDouble(out->ctrl, (*e.ctrl)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Light& e, mjsLight* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.type.has_value()) { out->type = static_cast<mjtLightType>(ToMjt((*e.type))); }
  if (e.castshadow.has_value()) { out->castshadow = (*e.castshadow); }
  if (e.active.has_value()) { out->active = (*e.active); }
  if (e.pos.has_value()) { for (int i = 0; i < 3; ++i) out->pos[i] = (*e.pos)[i]; }
  if (e.dir.has_value()) { for (int i = 0; i < 3; ++i) out->dir[i] = (*e.dir)[i]; }
  if (e.bulbradius.has_value()) { out->bulbradius = (*e.bulbradius); }
  if (e.intensity.has_value()) { out->intensity = (*e.intensity); }
  if (e.range.has_value()) { out->range = (*e.range); }
  if (e.attenuation.has_value()) { for (int i = 0; i < 3; ++i) out->attenuation[i] = (*e.attenuation)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.exponent.has_value()) { out->exponent = (*e.exponent); }
  if (e.ambient.has_value()) { for (int i = 0; i < 3; ++i) out->ambient[i] = (*e.ambient)[i]; }
  if (e.diffuse.has_value()) { for (int i = 0; i < 3; ++i) out->diffuse[i] = (*e.diffuse)[i]; }
  if (e.specular.has_value()) { for (int i = 0; i < 3; ++i) out->specular[i] = (*e.specular)[i]; }
  if (e.mode.has_value()) { out->mode = static_cast<mjtCamLight>(ToMjt((*e.mode))); }
  if (e.target.has_value()) { mjs_setString(out->targetbody, (*e.target).name.c_str()); }
  if (e.texture.has_value()) { mjs_setString(out->texture, (*e.texture).name.c_str()); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Magnetometer& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Material& e, mjsMaterial* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.texrepeat.has_value()) { for (int i = 0; i < 2; ++i) out->texrepeat[i] = (*e.texrepeat)[i]; }
  if (e.texuniform.has_value()) { out->texuniform = (*e.texuniform); }
  if (e.emission.has_value()) { out->emission = (*e.emission); }
  if (e.specular.has_value()) { out->specular = (*e.specular); }
  if (e.shininess.has_value()) { out->shininess = (*e.shininess); }
  if (e.reflectance.has_value()) { out->reflectance = (*e.reflectance); }
  if (e.metallic.has_value()) { out->metallic = (*e.metallic); }
  if (e.roughness.has_value()) { out->roughness = (*e.roughness); }
  if (e.rgba.has_value()) { for (int i = 0; i < 4; ++i) out->rgba[i] = (*e.rgba)[i]; }
  (void)e;
  (void)out;
}

void ApplyMjs(const Mesh& e, mjsMesh* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.content_type.has_value()) { mjs_setString(out->content_type, (*e.content_type).c_str()); }
  if (e.file.has_value()) { mjs_setString(out->file, (*e.file).c_str()); }
  if (e.vertex.has_value()) { ps::mjcf::compile::SetFloat(out->uservert, (*e.vertex)); }
  if (e.normal.has_value()) { ps::mjcf::compile::SetFloat(out->usernormal, (*e.normal)); }
  if (e.texcoord.has_value()) { ps::mjcf::compile::SetFloat(out->usertexcoord, (*e.texcoord)); }
  if (e.face.has_value()) { ps::mjcf::compile::SetInt(out->userface, (*e.face)); }
  if (e.refpos.has_value()) { for (int i = 0; i < 3; ++i) out->refpos[i] = (*e.refpos)[i]; }
  if (e.refquat.has_value()) { for (int i = 0; i < 4; ++i) out->refquat[i] = (*e.refquat)[i]; }
  if (e.scale.has_value()) { for (int i = 0; i < 3; ++i) out->scale[i] = (*e.scale)[i]; }
  if (e.smoothnormal.has_value()) { out->smoothnormal = (*e.smoothnormal); }
  if (e.maxhullvert.has_value()) { out->maxhullvert = (*e.maxhullvert); }
  if (e.inertia.has_value()) { out->inertia = static_cast<mjtMeshInertia>(ToMjt((*e.inertia))); }
  if (e.material.has_value()) { mjs_setString(out->material, (*e.material).name.c_str()); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Motor& e, mjsActuator* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.group.has_value()) { out->group = (*e.group); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.ctrllimited.has_value()) { out->ctrllimited = static_cast<int>(ToMjt((*e.ctrllimited))); }
  if (e.forcelimited.has_value()) { out->forcelimited = static_cast<int>(ToMjt((*e.forcelimited))); }
  if (e.ctrlrange.has_value()) { for (int i = 0; i < 2; ++i) out->ctrlrange[i] = (*e.ctrlrange)[i]; }
  if (e.forcerange.has_value()) { for (int i = 0; i < 2; ++i) out->forcerange[i] = (*e.forcerange)[i]; }
  if (e.lengthrange.has_value()) { for (int i = 0; i < 2; ++i) out->lengthrange[i] = (*e.lengthrange)[i]; }
  if (e.gear.has_value()) { for (std::size_t i = 0; i < (*e.gear).size(); ++i) out->gear[i] = (*e.gear)[i]; }
  if (e.damping.has_value()) { for (std::size_t i = 0; i < (*e.damping).size(); ++i) out->damping[i] = (*e.damping)[i]; }
  if (e.armature.has_value()) { out->armature = (*e.armature); }
  if (e.cranklength.has_value()) { out->cranklength = (*e.cranklength); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  if (e.slidersite.has_value()) { mjs_setString(out->slidersite, (*e.slidersite).name.c_str()); }
  if (e.refsite.has_value()) { mjs_setString(out->refsite, (*e.refsite).name.c_str()); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Muscle& e, mjsActuator* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.group.has_value()) { out->group = (*e.group); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.ctrllimited.has_value()) { out->ctrllimited = static_cast<int>(ToMjt((*e.ctrllimited))); }
  if (e.forcelimited.has_value()) { out->forcelimited = static_cast<int>(ToMjt((*e.forcelimited))); }
  if (e.ctrlrange.has_value()) { for (int i = 0; i < 2; ++i) out->ctrlrange[i] = (*e.ctrlrange)[i]; }
  if (e.forcerange.has_value()) { for (int i = 0; i < 2; ++i) out->forcerange[i] = (*e.forcerange)[i]; }
  if (e.lengthrange.has_value()) { for (int i = 0; i < 2; ++i) out->lengthrange[i] = (*e.lengthrange)[i]; }
  if (e.gear.has_value()) { for (std::size_t i = 0; i < (*e.gear).size(); ++i) out->gear[i] = (*e.gear)[i]; }
  if (e.damping.has_value()) { for (std::size_t i = 0; i < (*e.damping).size(); ++i) out->damping[i] = (*e.damping)[i]; }
  if (e.armature.has_value()) { out->armature = (*e.armature); }
  if (e.cranklength.has_value()) { out->cranklength = (*e.cranklength); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  if (e.slidersite.has_value()) { mjs_setString(out->slidersite, (*e.slidersite).name.c_str()); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Normal& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Numeric& e, mjsNumeric* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.data.has_value()) { ps::mjcf::compile::SetDouble(out->data, (*e.data)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Pair& e, mjsPair* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.geom1.has_value()) { mjs_setString(out->geomname1, (*e.geom1).name.c_str()); }
  if (e.geom2.has_value()) { mjs_setString(out->geomname2, (*e.geom2).name.c_str()); }
  if (e.condim.has_value()) { out->condim = (*e.condim); }
  if (e.friction.has_value()) { for (std::size_t i = 0; i < (*e.friction).size(); ++i) out->friction[i] = (*e.friction)[i]; }
  if (e.solref.has_value()) { for (std::size_t i = 0; i < (*e.solref).size(); ++i) out->solref[i] = (*e.solref)[i]; }
  if (e.solreffriction.has_value()) { for (std::size_t i = 0; i < (*e.solreffriction).size(); ++i) out->solreffriction[i] = (*e.solreffriction)[i]; }
  if (e.solimp.has_value()) { for (std::size_t i = 0; i < (*e.solimp).size(); ++i) out->solimp[i] = (*e.solimp)[i]; }
  if (e.gap.has_value()) { out->gap = (*e.gap); }
  if (e.margin.has_value()) { out->margin = (*e.margin); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Position& e, mjsActuator* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.group.has_value()) { out->group = (*e.group); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.ctrllimited.has_value()) { out->ctrllimited = static_cast<int>(ToMjt((*e.ctrllimited))); }
  if (e.forcelimited.has_value()) { out->forcelimited = static_cast<int>(ToMjt((*e.forcelimited))); }
  if (e.ctrlrange.has_value()) { for (int i = 0; i < 2; ++i) out->ctrlrange[i] = (*e.ctrlrange)[i]; }
  if (e.forcerange.has_value()) { for (int i = 0; i < 2; ++i) out->forcerange[i] = (*e.forcerange)[i]; }
  if (e.lengthrange.has_value()) { for (int i = 0; i < 2; ++i) out->lengthrange[i] = (*e.lengthrange)[i]; }
  if (e.gear.has_value()) { for (std::size_t i = 0; i < (*e.gear).size(); ++i) out->gear[i] = (*e.gear)[i]; }
  if (e.damping.has_value()) { for (std::size_t i = 0; i < (*e.damping).size(); ++i) out->damping[i] = (*e.damping)[i]; }
  if (e.armature.has_value()) { out->armature = (*e.armature); }
  if (e.cranklength.has_value()) { out->cranklength = (*e.cranklength); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  if (e.slidersite.has_value()) { mjs_setString(out->slidersite, (*e.slidersite).name.c_str()); }
  if (e.refsite.has_value()) { mjs_setString(out->refsite, (*e.refsite).name.c_str()); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Rangefinder& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const SensorContact& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const SensorPlugin& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.objname.has_value()) { mjs_setString(out->objname, (*e.objname).c_str()); }
  if (e.refname.has_value()) { mjs_setString(out->refname, (*e.refname).c_str()); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const SensorUser& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.objname.has_value()) { mjs_setString(out->objname, (*e.objname).c_str()); }
  if (e.datatype.has_value()) { out->datatype = static_cast<mjtDataType>(ToMjt((*e.datatype))); }
  if (e.needstage.has_value()) { out->needstage = static_cast<mjtStage>(ToMjt((*e.needstage))); }
  if (e.dim.has_value()) { out->dim = (*e.dim); }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Site& e, mjsSite* out) {
  if (e.pos.has_value()) { for (int i = 0; i < 3; ++i) out->pos[i] = (*e.pos)[i]; }
  if (e.quat.has_value()) { for (int i = 0; i < 4; ++i) out->quat[i] = (*e.quat)[i]; }
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.type.has_value()) { out->type = static_cast<mjtGeom>(ToMjt((*e.type))); }
  if (e.group.has_value()) { out->group = (*e.group); }
  if (e.material.has_value()) { mjs_setString(out->material, (*e.material).name.c_str()); }
  if (e.size.has_value()) { for (std::size_t i = 0; i < (*e.size).size(); ++i) out->size[i] = (*e.size)[i]; }
  if (e.rgba.has_value()) { for (int i = 0; i < 4; ++i) out->rgba[i] = (*e.rgba)[i]; }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  if (e.shape.has_value()) { ps::mjcf::compile::ApplyShape((*e.shape), out); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Skin& e, mjsSkin* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.file.has_value()) { mjs_setString(out->file, (*e.file).c_str()); }
  if (e.material.has_value()) { mjs_setString(out->material, (*e.material).name.c_str()); }
  if (e.rgba.has_value()) { for (int i = 0; i < 4; ++i) out->rgba[i] = (*e.rgba)[i]; }
  if (e.inflate.has_value()) { out->inflate = (*e.inflate); }
  if (e.vertex.has_value()) { ps::mjcf::compile::SetFloat(out->vert, (*e.vertex)); }
  if (e.texcoord.has_value()) { ps::mjcf::compile::SetFloat(out->texcoord, (*e.texcoord)); }
  if (e.face.has_value()) { ps::mjcf::compile::SetInt(out->face, (*e.face)); }
  if (e.group.has_value()) { out->group = (*e.group); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Spatial& e, mjsTendon* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.group.has_value()) { out->group = (*e.group); }
  if (e.limited.has_value()) { out->limited = static_cast<int>(ToMjt((*e.limited))); }
  if (e.actuatorfrclimited.has_value()) { out->actfrclimited = static_cast<int>(ToMjt((*e.actuatorfrclimited))); }
  if (e.range.has_value()) { for (int i = 0; i < 2; ++i) out->range[i] = (*e.range)[i]; }
  if (e.actuatorfrcrange.has_value()) { for (int i = 0; i < 2; ++i) out->actfrcrange[i] = (*e.actuatorfrcrange)[i]; }
  if (e.solreflimit.has_value()) { for (std::size_t i = 0; i < (*e.solreflimit).size(); ++i) out->solref_limit[i] = (*e.solreflimit)[i]; }
  if (e.solimplimit.has_value()) { for (std::size_t i = 0; i < (*e.solimplimit).size(); ++i) out->solimp_limit[i] = (*e.solimplimit)[i]; }
  if (e.solreffriction.has_value()) { for (std::size_t i = 0; i < (*e.solreffriction).size(); ++i) out->solref_friction[i] = (*e.solreffriction)[i]; }
  if (e.solimpfriction.has_value()) { for (std::size_t i = 0; i < (*e.solimpfriction).size(); ++i) out->solimp_friction[i] = (*e.solimpfriction)[i]; }
  if (e.frictionloss.has_value()) { out->frictionloss = (*e.frictionloss); }
  if (e.springlength.has_value()) { for (int i = 0; i < 2; ++i) out->springlength[i] = (*e.springlength)[i]; }
  if (e.width.has_value()) { out->width = (*e.width); }
  if (e.material.has_value()) { mjs_setString(out->material, (*e.material).name.c_str()); }
  if (e.margin.has_value()) { out->margin = (*e.margin); }
  if (e.stiffness.has_value()) { for (std::size_t i = 0; i < (*e.stiffness).size(); ++i) out->stiffness[i] = (*e.stiffness)[i]; }
  if (e.damping.has_value()) { for (std::size_t i = 0; i < (*e.damping).size(); ++i) out->damping[i] = (*e.damping)[i]; }
  if (e.armature.has_value()) { out->armature = (*e.armature); }
  if (e.rgba.has_value()) { for (int i = 0; i < 4; ++i) out->rgba[i] = (*e.rgba)[i]; }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Subtreeangmom& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Subtreecom& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Subtreelinvel& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Tactile& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const TendonDefault& e, mjsTendon* out) {
  if (e.group.has_value()) { out->group = (*e.group); }
  if (e.limited.has_value()) { out->limited = static_cast<int>(ToMjt((*e.limited))); }
  if (e.range.has_value()) { for (int i = 0; i < 2; ++i) out->range[i] = (*e.range)[i]; }
  if (e.solreflimit.has_value()) { for (std::size_t i = 0; i < (*e.solreflimit).size(); ++i) out->solref_limit[i] = (*e.solreflimit)[i]; }
  if (e.solimplimit.has_value()) { for (std::size_t i = 0; i < (*e.solimplimit).size(); ++i) out->solimp_limit[i] = (*e.solimplimit)[i]; }
  if (e.solreffriction.has_value()) { for (std::size_t i = 0; i < (*e.solreffriction).size(); ++i) out->solref_friction[i] = (*e.solreffriction)[i]; }
  if (e.solimpfriction.has_value()) { for (std::size_t i = 0; i < (*e.solimpfriction).size(); ++i) out->solimp_friction[i] = (*e.solimpfriction)[i]; }
  if (e.frictionloss.has_value()) { out->frictionloss = (*e.frictionloss); }
  if (e.springlength.has_value()) { for (int i = 0; i < 2; ++i) out->springlength[i] = (*e.springlength)[i]; }
  if (e.width.has_value()) { out->width = (*e.width); }
  if (e.material.has_value()) { mjs_setString(out->material, (*e.material).name.c_str()); }
  if (e.margin.has_value()) { out->margin = (*e.margin); }
  if (e.stiffness.has_value()) { for (std::size_t i = 0; i < (*e.stiffness).size(); ++i) out->stiffness[i] = (*e.stiffness)[i]; }
  if (e.damping.has_value()) { for (std::size_t i = 0; i < (*e.damping).size(); ++i) out->damping[i] = (*e.damping)[i]; }
  if (e.rgba.has_value()) { for (int i = 0; i < 4; ++i) out->rgba[i] = (*e.rgba)[i]; }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Tendonactuatorfrc& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Tendonlimitfrc& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Tendonlimitpos& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Tendonlimitvel& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Tendonpos& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Tendonvel& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Text& e, mjsText* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.data.has_value()) { mjs_setString(out->data, (*e.data).c_str()); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Texture& e, mjsTexture* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.type.has_value()) { out->type = static_cast<mjtTexture>(ToMjt((*e.type))); }
  if (e.colorspace.has_value()) { out->colorspace = static_cast<mjtColorSpace>(ToMjt((*e.colorspace))); }
  if (e.content_type.has_value()) { mjs_setString(out->content_type, (*e.content_type).c_str()); }
  if (e.gridsize.has_value()) { for (int i = 0; i < 2; ++i) out->gridsize[i] = (*e.gridsize)[i]; }
  if (e.rgb1.has_value()) { for (int i = 0; i < 3; ++i) out->rgb1[i] = (*e.rgb1)[i]; }
  if (e.rgb2.has_value()) { for (int i = 0; i < 3; ++i) out->rgb2[i] = (*e.rgb2)[i]; }
  if (e.mark.has_value()) { out->mark = static_cast<int>(ToMjt((*e.mark))); }
  if (e.markrgb.has_value()) { for (int i = 0; i < 3; ++i) out->markrgb[i] = (*e.markrgb)[i]; }
  if (e.random.has_value()) { out->random = (*e.random); }
  if (e.width.has_value()) { out->width = (*e.width); }
  if (e.height.has_value()) { out->height = (*e.height); }
  if (e.hflip.has_value()) { out->hflip = (*e.hflip); }
  if (e.vflip.has_value()) { out->vflip = (*e.vflip); }
  if (e.nchannel.has_value()) { out->nchannel = (*e.nchannel); }
  if (e.source.has_value()) { ps::mjcf::compile::ApplyTextureSource((*e.source), out); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Torque& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Touch& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Tuple& e, mjsTuple* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Velocimeter& e, mjsSensor* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.interval.has_value()) { for (std::size_t i = 0; i < (*e.interval).size(); ++i) out->interval[i] = (*e.interval)[i]; }
  if (e.cutoff.has_value()) { out->cutoff = (*e.cutoff); }
  if (e.noise.has_value()) { out->noise = (*e.noise); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Velocity& e, mjsActuator* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.group.has_value()) { out->group = (*e.group); }
  if (e.nsample.has_value()) { out->nsample = (*e.nsample); }
  if (e.interp.has_value()) { out->interp = static_cast<int>(ToMjt((*e.interp))); }
  if (e.delay.has_value()) { out->delay = (*e.delay); }
  if (e.ctrllimited.has_value()) { out->ctrllimited = static_cast<int>(ToMjt((*e.ctrllimited))); }
  if (e.forcelimited.has_value()) { out->forcelimited = static_cast<int>(ToMjt((*e.forcelimited))); }
  if (e.ctrlrange.has_value()) { for (int i = 0; i < 2; ++i) out->ctrlrange[i] = (*e.ctrlrange)[i]; }
  if (e.forcerange.has_value()) { for (int i = 0; i < 2; ++i) out->forcerange[i] = (*e.forcerange)[i]; }
  if (e.lengthrange.has_value()) { for (int i = 0; i < 2; ++i) out->lengthrange[i] = (*e.lengthrange)[i]; }
  if (e.gear.has_value()) { for (std::size_t i = 0; i < (*e.gear).size(); ++i) out->gear[i] = (*e.gear)[i]; }
  if (e.damping.has_value()) { for (std::size_t i = 0; i < (*e.damping).size(); ++i) out->damping[i] = (*e.damping)[i]; }
  if (e.armature.has_value()) { out->armature = (*e.armature); }
  if (e.cranklength.has_value()) { out->cranklength = (*e.cranklength); }
  if (e.user.has_value()) { ps::mjcf::compile::SetDouble(out->userdata, (*e.user)); }
  if (e.slidersite.has_value()) { mjs_setString(out->slidersite, (*e.slidersite).name.c_str()); }
  if (e.refsite.has_value()) { mjs_setString(out->refsite, (*e.refsite).name.c_str()); }
  (void)e;
  (void)out;
}

void ApplyMjs(const Weld& e, mjsEquality* out) {
  if (e.name.has_value()) { mjs_setName(out->element, (*e.name).c_str()); }
  if (e.active.has_value()) { out->active = (*e.active); }
  if (e.solref.has_value()) { for (std::size_t i = 0; i < (*e.solref).size(); ++i) out->solref[i] = (*e.solref)[i]; }
  if (e.solimp.has_value()) { for (std::size_t i = 0; i < (*e.solimp).size(); ++i) out->solimp[i] = (*e.solimp)[i]; }
  (void)e;
  (void)out;
}

}  // namespace ps::mjcf
