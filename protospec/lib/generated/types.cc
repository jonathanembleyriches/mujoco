// Generated by protospec_gen.emit — do not edit.
#include "types.h"

#include <type_traits>
#include <utility>
#include <variant>

namespace ps::mjcf {

ActuatorAny Clone(const ActuatorAny& src) {
  ActuatorAny out;
  std::visit(
      [&](const auto& p) {
        if (p) out.node = Clone(*p);
      },
      src.node);
  return out;
}

bool operator==(const ActuatorAny& a, const ActuatorAny& b) {
  if (a.node.index() != b.node.index()) return false;
  return std::visit(
      [&](const auto& pa) {
        const auto& pb = std::get<std::decay_t<decltype(pa)>>(b.node);
        const bool ha = static_cast<bool>(pa);
        const bool hb = static_cast<bool>(pb);
        if (ha != hb) return false;
        return !ha || (*pa == *pb);
      },
      a.node);
}

BodyChildAny Clone(const BodyChildAny& src) {
  BodyChildAny out;
  std::visit(
      [&](const auto& p) {
        if (p) out.node = Clone(*p);
      },
      src.node);
  return out;
}

bool operator==(const BodyChildAny& a, const BodyChildAny& b) {
  if (a.node.index() != b.node.index()) return false;
  return std::visit(
      [&](const auto& pa) {
        const auto& pb = std::get<std::decay_t<decltype(pa)>>(b.node);
        const bool ha = static_cast<bool>(pa);
        const bool hb = static_cast<bool>(pb);
        if (ha != hb) return false;
        return !ha || (*pa == *pb);
      },
      a.node);
}

EqualityAny Clone(const EqualityAny& src) {
  EqualityAny out;
  std::visit(
      [&](const auto& p) {
        if (p) out.node = Clone(*p);
      },
      src.node);
  return out;
}

bool operator==(const EqualityAny& a, const EqualityAny& b) {
  if (a.node.index() != b.node.index()) return false;
  return std::visit(
      [&](const auto& pa) {
        const auto& pb = std::get<std::decay_t<decltype(pa)>>(b.node);
        const bool ha = static_cast<bool>(pa);
        const bool hb = static_cast<bool>(pb);
        if (ha != hb) return false;
        return !ha || (*pa == *pb);
      },
      a.node);
}

PathItemAny Clone(const PathItemAny& src) {
  PathItemAny out;
  std::visit(
      [&](const auto& p) {
        if (p) out.node = Clone(*p);
      },
      src.node);
  return out;
}

bool operator==(const PathItemAny& a, const PathItemAny& b) {
  if (a.node.index() != b.node.index()) return false;
  return std::visit(
      [&](const auto& pa) {
        const auto& pb = std::get<std::decay_t<decltype(pa)>>(b.node);
        const bool ha = static_cast<bool>(pa);
        const bool hb = static_cast<bool>(pb);
        if (ha != hb) return false;
        return !ha || (*pa == *pb);
      },
      a.node);
}

SensorAny Clone(const SensorAny& src) {
  SensorAny out;
  std::visit(
      [&](const auto& p) {
        if (p) out.node = Clone(*p);
      },
      src.node);
  return out;
}

bool operator==(const SensorAny& a, const SensorAny& b) {
  if (a.node.index() != b.node.index()) return false;
  return std::visit(
      [&](const auto& pa) {
        const auto& pb = std::get<std::decay_t<decltype(pa)>>(b.node);
        const bool ha = static_cast<bool>(pa);
        const bool hb = static_cast<bool>(pb);
        if (ha != hb) return false;
        return !ha || (*pa == *pb);
      },
      a.node);
}

TendonAny Clone(const TendonAny& src) {
  TendonAny out;
  std::visit(
      [&](const auto& p) {
        if (p) out.node = Clone(*p);
      },
      src.node);
  return out;
}

bool operator==(const TendonAny& a, const TendonAny& b) {
  if (a.node.index() != b.node.index()) return false;
  return std::visit(
      [&](const auto& pa) {
        const auto& pb = std::get<std::decay_t<decltype(pa)>>(b.node);
        const bool ha = static_cast<bool>(pa);
        const bool hb = static_cast<bool>(pb);
        if (ha != hb) return false;
        return !ha || (*pa == *pb);
      },
      a.node);
}

std::unique_ptr<Accelerometer> Clone(const Accelerometer& src) {
  auto out = std::make_unique<Accelerometer>();
  out->loc = src.loc;
  out->name = src.name;
  out->site = src.site;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Accelerometer& a, const Accelerometer& b) {
  return a.name == b.name &&
         a.site == b.site &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Actuator> Clone(const Actuator& src) {
  auto out = std::make_unique<Actuator>();
  out->loc = src.loc;
  for (const auto& item : src.actuators)
    out->actuators.push_back(Clone(item));
  return out;
}

bool operator==(const Actuator& a, const Actuator& b) {
  return a.actuators == b.actuators;
}

std::unique_ptr<ActuatorGeneral> Clone(const ActuatorGeneral& src) {
  auto out = std::make_unique<ActuatorGeneral>();
  out->loc = src.loc;
  out->name = src.name;
  out->dclass = src.dclass;
  out->group = src.group;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->ctrllimited = src.ctrllimited;
  out->forcelimited = src.forcelimited;
  out->actlimited = src.actlimited;
  out->ctrlrange = src.ctrlrange;
  out->forcerange = src.forcerange;
  out->actrange = src.actrange;
  out->lengthrange = src.lengthrange;
  out->gear = src.gear;
  out->damping = src.damping;
  out->armature = src.armature;
  out->cranklength = src.cranklength;
  out->user = src.user;
  out->joint = src.joint;
  out->jointinparent = src.jointinparent;
  out->tendon = src.tendon;
  out->slidersite = src.slidersite;
  out->cranksite = src.cranksite;
  out->site = src.site;
  out->refsite = src.refsite;
  out->body = src.body;
  out->actdim = src.actdim;
  out->dyntype = src.dyntype;
  out->gaintype = src.gaintype;
  out->biastype = src.biastype;
  out->dynprm = src.dynprm;
  out->gainprm = src.gainprm;
  out->biasprm = src.biasprm;
  out->actearly = src.actearly;
  return out;
}

bool operator==(const ActuatorGeneral& a, const ActuatorGeneral& b) {
  return a.name == b.name &&
         a.dclass == b.dclass &&
         a.group == b.group &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.ctrllimited == b.ctrllimited &&
         a.forcelimited == b.forcelimited &&
         a.actlimited == b.actlimited &&
         a.ctrlrange == b.ctrlrange &&
         a.forcerange == b.forcerange &&
         a.actrange == b.actrange &&
         a.lengthrange == b.lengthrange &&
         a.gear == b.gear &&
         a.damping == b.damping &&
         a.armature == b.armature &&
         a.cranklength == b.cranklength &&
         a.user == b.user &&
         a.joint == b.joint &&
         a.jointinparent == b.jointinparent &&
         a.tendon == b.tendon &&
         a.slidersite == b.slidersite &&
         a.cranksite == b.cranksite &&
         a.site == b.site &&
         a.refsite == b.refsite &&
         a.body == b.body &&
         a.actdim == b.actdim &&
         a.dyntype == b.dyntype &&
         a.gaintype == b.gaintype &&
         a.biastype == b.biastype &&
         a.dynprm == b.dynprm &&
         a.gainprm == b.gainprm &&
         a.biasprm == b.biasprm &&
         a.actearly == b.actearly;
}

std::unique_ptr<ActuatorPlugin> Clone(const ActuatorPlugin& src) {
  auto out = std::make_unique<ActuatorPlugin>();
  out->loc = src.loc;
  out->name = src.name;
  out->dclass = src.dclass;
  out->plugin = src.plugin;
  out->instance = src.instance;
  out->group = src.group;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->ctrllimited = src.ctrllimited;
  out->forcelimited = src.forcelimited;
  out->actlimited = src.actlimited;
  out->ctrlrange = src.ctrlrange;
  out->forcerange = src.forcerange;
  out->actrange = src.actrange;
  out->lengthrange = src.lengthrange;
  out->gear = src.gear;
  out->damping = src.damping;
  out->armature = src.armature;
  out->cranklength = src.cranklength;
  out->joint = src.joint;
  out->jointinparent = src.jointinparent;
  out->site = src.site;
  out->actdim = src.actdim;
  out->dyntype = src.dyntype;
  out->dynprm = src.dynprm;
  out->tendon = src.tendon;
  out->cranksite = src.cranksite;
  out->slidersite = src.slidersite;
  out->user = src.user;
  out->actearly = src.actearly;
  out->config = ps::PtrVecClone(src.config);
  return out;
}

bool operator==(const ActuatorPlugin& a, const ActuatorPlugin& b) {
  return a.name == b.name &&
         a.dclass == b.dclass &&
         a.plugin == b.plugin &&
         a.instance == b.instance &&
         a.group == b.group &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.ctrllimited == b.ctrllimited &&
         a.forcelimited == b.forcelimited &&
         a.actlimited == b.actlimited &&
         a.ctrlrange == b.ctrlrange &&
         a.forcerange == b.forcerange &&
         a.actrange == b.actrange &&
         a.lengthrange == b.lengthrange &&
         a.gear == b.gear &&
         a.damping == b.damping &&
         a.armature == b.armature &&
         a.cranklength == b.cranklength &&
         a.joint == b.joint &&
         a.jointinparent == b.jointinparent &&
         a.site == b.site &&
         a.actdim == b.actdim &&
         a.dyntype == b.dyntype &&
         a.dynprm == b.dynprm &&
         a.tendon == b.tendon &&
         a.cranksite == b.cranksite &&
         a.slidersite == b.slidersite &&
         a.user == b.user &&
         a.actearly == b.actearly &&
         ps::PtrVecEq(a.config, b.config);
}

std::unique_ptr<Actuatorfrc> Clone(const Actuatorfrc& src) {
  auto out = std::make_unique<Actuatorfrc>();
  out->loc = src.loc;
  out->name = src.name;
  out->actuator = src.actuator;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Actuatorfrc& a, const Actuatorfrc& b) {
  return a.name == b.name &&
         a.actuator == b.actuator &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Actuatorpos> Clone(const Actuatorpos& src) {
  auto out = std::make_unique<Actuatorpos>();
  out->loc = src.loc;
  out->name = src.name;
  out->actuator = src.actuator;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Actuatorpos& a, const Actuatorpos& b) {
  return a.name == b.name &&
         a.actuator == b.actuator &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Actuatorvel> Clone(const Actuatorvel& src) {
  auto out = std::make_unique<Actuatorvel>();
  out->loc = src.loc;
  out->name = src.name;
  out->actuator = src.actuator;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Actuatorvel& a, const Actuatorvel& b) {
  return a.name == b.name &&
         a.actuator == b.actuator &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Adhesion> Clone(const Adhesion& src) {
  auto out = std::make_unique<Adhesion>();
  out->loc = src.loc;
  out->name = src.name;
  out->dclass = src.dclass;
  out->group = src.group;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->forcelimited = src.forcelimited;
  out->ctrlrange = src.ctrlrange;
  out->forcerange = src.forcerange;
  out->user = src.user;
  out->body = src.body;
  out->gain = src.gain;
  return out;
}

bool operator==(const Adhesion& a, const Adhesion& b) {
  return a.name == b.name &&
         a.dclass == b.dclass &&
         a.group == b.group &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.forcelimited == b.forcelimited &&
         a.ctrlrange == b.ctrlrange &&
         a.forcerange == b.forcerange &&
         a.user == b.user &&
         a.body == b.body &&
         a.gain == b.gain;
}

std::unique_ptr<Asset> Clone(const Asset& src) {
  auto out = std::make_unique<Asset>();
  out->loc = src.loc;
  out->meshs = ps::PtrVecClone(src.meshs);
  out->hfields = ps::PtrVecClone(src.hfields);
  out->skins = ps::PtrVecClone(src.skins);
  out->textures = ps::PtrVecClone(src.textures);
  out->materials = ps::PtrVecClone(src.materials);
  out->modelAssets = ps::PtrVecClone(src.modelAssets);
  return out;
}

bool operator==(const Asset& a, const Asset& b) {
  return ps::PtrVecEq(a.meshs, b.meshs) &&
         ps::PtrVecEq(a.hfields, b.hfields) &&
         ps::PtrVecEq(a.skins, b.skins) &&
         ps::PtrVecEq(a.textures, b.textures) &&
         ps::PtrVecEq(a.materials, b.materials) &&
         ps::PtrVecEq(a.modelAssets, b.modelAssets);
}

std::unique_ptr<Attach> Clone(const Attach& src) {
  auto out = std::make_unique<Attach>();
  out->loc = src.loc;
  out->model = src.model;
  out->body = src.body;
  out->frame = src.frame;
  out->prefix = src.prefix;
  return out;
}

bool operator==(const Attach& a, const Attach& b) {
  return a.model == b.model &&
         a.body == b.body &&
         a.frame == b.frame &&
         a.prefix == b.prefix;
}

std::unique_ptr<Ballangvel> Clone(const Ballangvel& src) {
  auto out = std::make_unique<Ballangvel>();
  out->loc = src.loc;
  out->name = src.name;
  out->joint = src.joint;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Ballangvel& a, const Ballangvel& b) {
  return a.name == b.name &&
         a.joint == b.joint &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Ballquat> Clone(const Ballquat& src) {
  auto out = std::make_unique<Ballquat>();
  out->loc = src.loc;
  out->name = src.name;
  out->joint = src.joint;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Ballquat& a, const Ballquat& b) {
  return a.name == b.name &&
         a.joint == b.joint &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Body> Clone(const Body& src) {
  auto out = std::make_unique<Body>();
  out->loc = src.loc;
  out->pos = src.pos;
  out->quat = src.quat;
  out->name = src.name;
  out->childclass = src.childclass;
  out->mocap = src.mocap;
  out->gravcomp = src.gravcomp;
  out->sleep = src.sleep;
  out->simple = src.simple;
  out->user = src.user;
  out->inertial = ps::PtrVecClone(src.inertial);
  for (const auto& item : src.subtree)
    out->subtree.push_back(Clone(item));
  return out;
}

bool operator==(const Body& a, const Body& b) {
  return a.pos == b.pos &&
         a.quat == b.quat &&
         a.name == b.name &&
         a.childclass == b.childclass &&
         a.mocap == b.mocap &&
         a.gravcomp == b.gravcomp &&
         a.sleep == b.sleep &&
         a.simple == b.simple &&
         a.user == b.user &&
         ps::PtrVecEq(a.inertial, b.inertial) &&
         a.subtree == b.subtree;
}

std::unique_ptr<Camera> Clone(const Camera& src) {
  auto out = std::make_unique<Camera>();
  out->loc = src.loc;
  out->pos = src.pos;
  out->quat = src.quat;
  out->name = src.name;
  out->dclass = src.dclass;
  out->projection = src.projection;
  out->ipd = src.ipd;
  out->resolution = src.resolution;
  out->output = src.output;
  out->mode = src.mode;
  out->target = src.target;
  out->focalpixel = src.focalpixel;
  out->principal = src.principal;
  out->principalpixel = src.principalpixel;
  out->sensorsize = src.sensorsize;
  out->user = src.user;
  out->fovy = src.fovy;
  out->focal = src.focal;
  return out;
}

bool operator==(const Camera& a, const Camera& b) {
  return a.pos == b.pos &&
         a.quat == b.quat &&
         a.name == b.name &&
         a.dclass == b.dclass &&
         a.projection == b.projection &&
         a.ipd == b.ipd &&
         a.resolution == b.resolution &&
         a.output == b.output &&
         a.mode == b.mode &&
         a.target == b.target &&
         a.focalpixel == b.focalpixel &&
         a.principal == b.principal &&
         a.principalpixel == b.principalpixel &&
         a.sensorsize == b.sensorsize &&
         a.user == b.user &&
         a.fovy == b.fovy &&
         a.focal == b.focal;
}

std::unique_ptr<Camprojection> Clone(const Camprojection& src) {
  auto out = std::make_unique<Camprojection>();
  out->loc = src.loc;
  out->name = src.name;
  out->site = src.site;
  out->camera = src.camera;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Camprojection& a, const Camprojection& b) {
  return a.name == b.name &&
         a.site == b.site &&
         a.camera == b.camera &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Clock> Clone(const Clock& src) {
  auto out = std::make_unique<Clock>();
  out->loc = src.loc;
  out->name = src.name;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Clock& a, const Clock& b) {
  return a.name == b.name &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Compiler> Clone(const Compiler& src) {
  auto out = std::make_unique<Compiler>();
  out->loc = src.loc;
  out->autolimits = src.autolimits;
  out->boundmass = src.boundmass;
  out->boundinertia = src.boundinertia;
  out->settotalmass = src.settotalmass;
  out->balanceinertia = src.balanceinertia;
  out->strippath = src.strippath;
  out->coordinate = src.coordinate;
  out->angle = src.angle;
  out->fitaabb = src.fitaabb;
  out->eulerseq = src.eulerseq;
  out->meshdir = src.meshdir;
  out->texturedir = src.texturedir;
  out->discardvisual = src.discardvisual;
  out->usethread = src.usethread;
  out->fusestatic = src.fusestatic;
  out->inertiafromgeom = src.inertiafromgeom;
  out->inertiagrouprange = src.inertiagrouprange;
  out->saveinertial = src.saveinertial;
  out->assetdir = src.assetdir;
  out->alignfree = src.alignfree;
  out->conflict = src.conflict;
  out->lengthRanges = ps::PtrVecClone(src.lengthRanges);
  return out;
}

bool operator==(const Compiler& a, const Compiler& b) {
  return a.autolimits == b.autolimits &&
         a.boundmass == b.boundmass &&
         a.boundinertia == b.boundinertia &&
         a.settotalmass == b.settotalmass &&
         a.balanceinertia == b.balanceinertia &&
         a.strippath == b.strippath &&
         a.coordinate == b.coordinate &&
         a.angle == b.angle &&
         a.fitaabb == b.fitaabb &&
         a.eulerseq == b.eulerseq &&
         a.meshdir == b.meshdir &&
         a.texturedir == b.texturedir &&
         a.discardvisual == b.discardvisual &&
         a.usethread == b.usethread &&
         a.fusestatic == b.fusestatic &&
         a.inertiafromgeom == b.inertiafromgeom &&
         a.inertiagrouprange == b.inertiagrouprange &&
         a.saveinertial == b.saveinertial &&
         a.assetdir == b.assetdir &&
         a.alignfree == b.alignfree &&
         a.conflict == b.conflict &&
         ps::PtrVecEq(a.lengthRanges, b.lengthRanges);
}

std::unique_ptr<Composite> Clone(const Composite& src) {
  auto out = std::make_unique<Composite>();
  out->loc = src.loc;
  out->prefix = src.prefix;
  out->type = src.type;
  out->count = src.count;
  out->offset = src.offset;
  out->vertex = src.vertex;
  out->initial = src.initial;
  out->curve = src.curve;
  out->size = src.size;
  out->quat = src.quat;
  out->compositeJoints = ps::PtrVecClone(src.compositeJoints);
  out->compositeSkins = ps::PtrVecClone(src.compositeSkins);
  out->compositeGeoms = ps::PtrVecClone(src.compositeGeoms);
  out->compositeSites = ps::PtrVecClone(src.compositeSites);
  out->plugin = ps::PtrVecClone(src.plugin);
  return out;
}

bool operator==(const Composite& a, const Composite& b) {
  return a.prefix == b.prefix &&
         a.type == b.type &&
         a.count == b.count &&
         a.offset == b.offset &&
         a.vertex == b.vertex &&
         a.initial == b.initial &&
         a.curve == b.curve &&
         a.size == b.size &&
         a.quat == b.quat &&
         ps::PtrVecEq(a.compositeJoints, b.compositeJoints) &&
         ps::PtrVecEq(a.compositeSkins, b.compositeSkins) &&
         ps::PtrVecEq(a.compositeGeoms, b.compositeGeoms) &&
         ps::PtrVecEq(a.compositeSites, b.compositeSites) &&
         ps::PtrVecEq(a.plugin, b.plugin);
}

std::unique_ptr<CompositeGeom> Clone(const CompositeGeom& src) {
  auto out = std::make_unique<CompositeGeom>();
  out->loc = src.loc;
  out->type = src.type;
  out->contype = src.contype;
  out->conaffinity = src.conaffinity;
  out->condim = src.condim;
  out->group = src.group;
  out->priority = src.priority;
  out->size = src.size;
  out->material = src.material;
  out->rgba = src.rgba;
  out->friction = src.friction;
  out->mass = src.mass;
  out->density = src.density;
  out->solmix = src.solmix;
  out->solref = src.solref;
  out->solimp = src.solimp;
  out->margin = src.margin;
  out->gap = src.gap;
  out->surfacevel = src.surfacevel;
  return out;
}

bool operator==(const CompositeGeom& a, const CompositeGeom& b) {
  return a.type == b.type &&
         a.contype == b.contype &&
         a.conaffinity == b.conaffinity &&
         a.condim == b.condim &&
         a.group == b.group &&
         a.priority == b.priority &&
         a.size == b.size &&
         a.material == b.material &&
         a.rgba == b.rgba &&
         a.friction == b.friction &&
         a.mass == b.mass &&
         a.density == b.density &&
         a.solmix == b.solmix &&
         a.solref == b.solref &&
         a.solimp == b.solimp &&
         a.margin == b.margin &&
         a.gap == b.gap &&
         a.surfacevel == b.surfacevel;
}

std::unique_ptr<CompositeJoint> Clone(const CompositeJoint& src) {
  auto out = std::make_unique<CompositeJoint>();
  out->loc = src.loc;
  out->kind = src.kind;
  out->group = src.group;
  out->stiffness = src.stiffness;
  out->damping = src.damping;
  out->armature = src.armature;
  out->solreffix = src.solreffix;
  out->solimpfix = src.solimpfix;
  out->type = src.type;
  out->axis = src.axis;
  out->limited = src.limited;
  out->range = src.range;
  out->margin = src.margin;
  out->solreflimit = src.solreflimit;
  out->solimplimit = src.solimplimit;
  out->frictionloss = src.frictionloss;
  out->solreffriction = src.solreffriction;
  out->solimpfriction = src.solimpfriction;
  return out;
}

bool operator==(const CompositeJoint& a, const CompositeJoint& b) {
  return a.kind == b.kind &&
         a.group == b.group &&
         a.stiffness == b.stiffness &&
         a.damping == b.damping &&
         a.armature == b.armature &&
         a.solreffix == b.solreffix &&
         a.solimpfix == b.solimpfix &&
         a.type == b.type &&
         a.axis == b.axis &&
         a.limited == b.limited &&
         a.range == b.range &&
         a.margin == b.margin &&
         a.solreflimit == b.solreflimit &&
         a.solimplimit == b.solimplimit &&
         a.frictionloss == b.frictionloss &&
         a.solreffriction == b.solreffriction &&
         a.solimpfriction == b.solimpfriction;
}

std::unique_ptr<CompositeSite> Clone(const CompositeSite& src) {
  auto out = std::make_unique<CompositeSite>();
  out->loc = src.loc;
  out->group = src.group;
  out->size = src.size;
  out->material = src.material;
  out->rgba = src.rgba;
  return out;
}

bool operator==(const CompositeSite& a, const CompositeSite& b) {
  return a.group == b.group &&
         a.size == b.size &&
         a.material == b.material &&
         a.rgba == b.rgba;
}

std::unique_ptr<CompositeSkin> Clone(const CompositeSkin& src) {
  auto out = std::make_unique<CompositeSkin>();
  out->loc = src.loc;
  out->texcoord = src.texcoord;
  out->material = src.material;
  out->group = src.group;
  out->rgba = src.rgba;
  out->inflate = src.inflate;
  out->subgrid = src.subgrid;
  return out;
}

bool operator==(const CompositeSkin& a, const CompositeSkin& b) {
  return a.texcoord == b.texcoord &&
         a.material == b.material &&
         a.group == b.group &&
         a.rgba == b.rgba &&
         a.inflate == b.inflate &&
         a.subgrid == b.subgrid;
}

std::unique_ptr<Config> Clone(const Config& src) {
  auto out = std::make_unique<Config>();
  out->loc = src.loc;
  out->key = src.key;
  out->value = src.value;
  return out;
}

bool operator==(const Config& a, const Config& b) {
  return a.key == b.key &&
         a.value == b.value;
}

std::unique_ptr<Connect> Clone(const Connect& src) {
  auto out = std::make_unique<Connect>();
  out->loc = src.loc;
  out->name = src.name;
  out->dclass = src.dclass;
  out->body1 = src.body1;
  out->body2 = src.body2;
  out->anchor = src.anchor;
  out->site1 = src.site1;
  out->site2 = src.site2;
  out->active = src.active;
  out->solref = src.solref;
  out->solimp = src.solimp;
  return out;
}

bool operator==(const Connect& a, const Connect& b) {
  return a.name == b.name &&
         a.dclass == b.dclass &&
         a.body1 == b.body1 &&
         a.body2 == b.body2 &&
         a.anchor == b.anchor &&
         a.site1 == b.site1 &&
         a.site2 == b.site2 &&
         a.active == b.active &&
         a.solref == b.solref &&
         a.solimp == b.solimp;
}

std::unique_ptr<Contact> Clone(const Contact& src) {
  auto out = std::make_unique<Contact>();
  out->loc = src.loc;
  out->pairs = ps::PtrVecClone(src.pairs);
  out->excludes = ps::PtrVecClone(src.excludes);
  return out;
}

bool operator==(const Contact& a, const Contact& b) {
  return ps::PtrVecEq(a.pairs, b.pairs) &&
         ps::PtrVecEq(a.excludes, b.excludes);
}

std::unique_ptr<Custom> Clone(const Custom& src) {
  auto out = std::make_unique<Custom>();
  out->loc = src.loc;
  out->numerics = ps::PtrVecClone(src.numerics);
  out->texts = ps::PtrVecClone(src.texts);
  out->tuples = ps::PtrVecClone(src.tuples);
  return out;
}

bool operator==(const Custom& a, const Custom& b) {
  return ps::PtrVecEq(a.numerics, b.numerics) &&
         ps::PtrVecEq(a.texts, b.texts) &&
         ps::PtrVecEq(a.tuples, b.tuples);
}

std::unique_ptr<Cylinder> Clone(const Cylinder& src) {
  auto out = std::make_unique<Cylinder>();
  out->loc = src.loc;
  out->name = src.name;
  out->dclass = src.dclass;
  out->group = src.group;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->ctrllimited = src.ctrllimited;
  out->forcelimited = src.forcelimited;
  out->ctrlrange = src.ctrlrange;
  out->forcerange = src.forcerange;
  out->lengthrange = src.lengthrange;
  out->gear = src.gear;
  out->damping = src.damping;
  out->armature = src.armature;
  out->cranklength = src.cranklength;
  out->user = src.user;
  out->joint = src.joint;
  out->jointinparent = src.jointinparent;
  out->tendon = src.tendon;
  out->slidersite = src.slidersite;
  out->cranksite = src.cranksite;
  out->site = src.site;
  out->refsite = src.refsite;
  out->timeconst = src.timeconst;
  out->area = src.area;
  out->bias = src.bias;
  return out;
}

bool operator==(const Cylinder& a, const Cylinder& b) {
  return a.name == b.name &&
         a.dclass == b.dclass &&
         a.group == b.group &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.ctrllimited == b.ctrllimited &&
         a.forcelimited == b.forcelimited &&
         a.ctrlrange == b.ctrlrange &&
         a.forcerange == b.forcerange &&
         a.lengthrange == b.lengthrange &&
         a.gear == b.gear &&
         a.damping == b.damping &&
         a.armature == b.armature &&
         a.cranklength == b.cranklength &&
         a.user == b.user &&
         a.joint == b.joint &&
         a.jointinparent == b.jointinparent &&
         a.tendon == b.tendon &&
         a.slidersite == b.slidersite &&
         a.cranksite == b.cranksite &&
         a.site == b.site &&
         a.refsite == b.refsite &&
         a.timeconst == b.timeconst &&
         a.area == b.area &&
         a.bias == b.bias;
}

std::unique_ptr<Damper> Clone(const Damper& src) {
  auto out = std::make_unique<Damper>();
  out->loc = src.loc;
  out->name = src.name;
  out->dclass = src.dclass;
  out->group = src.group;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->forcelimited = src.forcelimited;
  out->ctrlrange = src.ctrlrange;
  out->forcerange = src.forcerange;
  out->lengthrange = src.lengthrange;
  out->gear = src.gear;
  out->damping = src.damping;
  out->armature = src.armature;
  out->cranklength = src.cranklength;
  out->user = src.user;
  out->joint = src.joint;
  out->jointinparent = src.jointinparent;
  out->tendon = src.tendon;
  out->slidersite = src.slidersite;
  out->cranksite = src.cranksite;
  out->site = src.site;
  out->refsite = src.refsite;
  out->kv = src.kv;
  return out;
}

bool operator==(const Damper& a, const Damper& b) {
  return a.name == b.name &&
         a.dclass == b.dclass &&
         a.group == b.group &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.forcelimited == b.forcelimited &&
         a.ctrlrange == b.ctrlrange &&
         a.forcerange == b.forcerange &&
         a.lengthrange == b.lengthrange &&
         a.gear == b.gear &&
         a.damping == b.damping &&
         a.armature == b.armature &&
         a.cranklength == b.cranklength &&
         a.user == b.user &&
         a.joint == b.joint &&
         a.jointinparent == b.jointinparent &&
         a.tendon == b.tendon &&
         a.slidersite == b.slidersite &&
         a.cranksite == b.cranksite &&
         a.site == b.site &&
         a.refsite == b.refsite &&
         a.kv == b.kv;
}

std::unique_ptr<DcMotor> Clone(const DcMotor& src) {
  auto out = std::make_unique<DcMotor>();
  out->loc = src.loc;
  out->name = src.name;
  out->dclass = src.dclass;
  out->group = src.group;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->ctrllimited = src.ctrllimited;
  out->ctrlrange = src.ctrlrange;
  out->lengthrange = src.lengthrange;
  out->gear = src.gear;
  out->damping = src.damping;
  out->armature = src.armature;
  out->cranklength = src.cranklength;
  out->user = src.user;
  out->joint = src.joint;
  out->jointinparent = src.jointinparent;
  out->tendon = src.tendon;
  out->slidersite = src.slidersite;
  out->cranksite = src.cranksite;
  out->site = src.site;
  out->refsite = src.refsite;
  out->motorconst = src.motorconst;
  out->resistance = src.resistance;
  out->nominal = src.nominal;
  out->saturation = src.saturation;
  out->inductance = src.inductance;
  out->cogging = src.cogging;
  out->controller = src.controller;
  out->thermal = src.thermal;
  out->lugre = src.lugre;
  out->input = src.input;
  return out;
}

bool operator==(const DcMotor& a, const DcMotor& b) {
  return a.name == b.name &&
         a.dclass == b.dclass &&
         a.group == b.group &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.ctrllimited == b.ctrllimited &&
         a.ctrlrange == b.ctrlrange &&
         a.lengthrange == b.lengthrange &&
         a.gear == b.gear &&
         a.damping == b.damping &&
         a.armature == b.armature &&
         a.cranklength == b.cranklength &&
         a.user == b.user &&
         a.joint == b.joint &&
         a.jointinparent == b.jointinparent &&
         a.tendon == b.tendon &&
         a.slidersite == b.slidersite &&
         a.cranksite == b.cranksite &&
         a.site == b.site &&
         a.refsite == b.refsite &&
         a.motorconst == b.motorconst &&
         a.resistance == b.resistance &&
         a.nominal == b.nominal &&
         a.saturation == b.saturation &&
         a.inductance == b.inductance &&
         a.cogging == b.cogging &&
         a.controller == b.controller &&
         a.thermal == b.thermal &&
         a.lugre == b.lugre &&
         a.input == b.input;
}

std::unique_ptr<Default> Clone(const Default& src) {
  auto out = std::make_unique<Default>();
  out->loc = src.loc;
  out->dclass = src.dclass;
  out->mesh = ps::PtrVecClone(src.mesh);
  out->material = ps::PtrVecClone(src.material);
  out->joint = ps::PtrVecClone(src.joint);
  out->geom = ps::PtrVecClone(src.geom);
  out->site = ps::PtrVecClone(src.site);
  out->camera = ps::PtrVecClone(src.camera);
  out->light = ps::PtrVecClone(src.light);
  out->pair = ps::PtrVecClone(src.pair);
  out->equality = ps::PtrVecClone(src.equality);
  out->tendon = ps::PtrVecClone(src.tendon);
  out->general = ps::PtrVecClone(src.general);
  out->motor = ps::PtrVecClone(src.motor);
  out->position = ps::PtrVecClone(src.position);
  out->velocity = ps::PtrVecClone(src.velocity);
  out->intvelocity = ps::PtrVecClone(src.intvelocity);
  out->damper = ps::PtrVecClone(src.damper);
  out->cylinder = ps::PtrVecClone(src.cylinder);
  out->muscle = ps::PtrVecClone(src.muscle);
  out->adhesion = ps::PtrVecClone(src.adhesion);
  out->dcmotor = ps::PtrVecClone(src.dcmotor);
  out->subclasses = ps::PtrVecClone(src.subclasses);
  return out;
}

bool operator==(const Default& a, const Default& b) {
  return a.dclass == b.dclass &&
         ps::PtrVecEq(a.mesh, b.mesh) &&
         ps::PtrVecEq(a.material, b.material) &&
         ps::PtrVecEq(a.joint, b.joint) &&
         ps::PtrVecEq(a.geom, b.geom) &&
         ps::PtrVecEq(a.site, b.site) &&
         ps::PtrVecEq(a.camera, b.camera) &&
         ps::PtrVecEq(a.light, b.light) &&
         ps::PtrVecEq(a.pair, b.pair) &&
         ps::PtrVecEq(a.equality, b.equality) &&
         ps::PtrVecEq(a.tendon, b.tendon) &&
         ps::PtrVecEq(a.general, b.general) &&
         ps::PtrVecEq(a.motor, b.motor) &&
         ps::PtrVecEq(a.position, b.position) &&
         ps::PtrVecEq(a.velocity, b.velocity) &&
         ps::PtrVecEq(a.intvelocity, b.intvelocity) &&
         ps::PtrVecEq(a.damper, b.damper) &&
         ps::PtrVecEq(a.cylinder, b.cylinder) &&
         ps::PtrVecEq(a.muscle, b.muscle) &&
         ps::PtrVecEq(a.adhesion, b.adhesion) &&
         ps::PtrVecEq(a.dcmotor, b.dcmotor) &&
         ps::PtrVecEq(a.subclasses, b.subclasses);
}

std::unique_ptr<Deformable> Clone(const Deformable& src) {
  auto out = std::make_unique<Deformable>();
  out->loc = src.loc;
  out->flexs = ps::PtrVecClone(src.flexs);
  out->skins = ps::PtrVecClone(src.skins);
  return out;
}

bool operator==(const Deformable& a, const Deformable& b) {
  return ps::PtrVecEq(a.flexs, b.flexs) &&
         ps::PtrVecEq(a.skins, b.skins);
}

std::unique_ptr<Distance> Clone(const Distance& src) {
  auto out = std::make_unique<Distance>();
  out->loc = src.loc;
  out->name = src.name;
  out->geom1 = src.geom1;
  out->geom2 = src.geom2;
  out->body1 = src.body1;
  out->body2 = src.body2;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Distance& a, const Distance& b) {
  return a.name == b.name &&
         a.geom1 == b.geom1 &&
         a.geom2 == b.geom2 &&
         a.body1 == b.body1 &&
         a.body2 == b.body2 &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<EKinetic> Clone(const EKinetic& src) {
  auto out = std::make_unique<EKinetic>();
  out->loc = src.loc;
  out->name = src.name;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const EKinetic& a, const EKinetic& b) {
  return a.name == b.name &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<EPotential> Clone(const EPotential& src) {
  auto out = std::make_unique<EPotential>();
  out->loc = src.loc;
  out->name = src.name;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const EPotential& a, const EPotential& b) {
  return a.name == b.name &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Equality> Clone(const Equality& src) {
  auto out = std::make_unique<Equality>();
  out->loc = src.loc;
  for (const auto& item : src.equalities)
    out->equalities.push_back(Clone(item));
  return out;
}

bool operator==(const Equality& a, const Equality& b) {
  return a.equalities == b.equalities;
}

std::unique_ptr<EqualityDefault> Clone(const EqualityDefault& src) {
  auto out = std::make_unique<EqualityDefault>();
  out->loc = src.loc;
  out->active = src.active;
  out->solref = src.solref;
  out->solimp = src.solimp;
  return out;
}

bool operator==(const EqualityDefault& a, const EqualityDefault& b) {
  return a.active == b.active &&
         a.solref == b.solref &&
         a.solimp == b.solimp;
}

std::unique_ptr<EqualityFlex> Clone(const EqualityFlex& src) {
  auto out = std::make_unique<EqualityFlex>();
  out->loc = src.loc;
  out->name = src.name;
  out->dclass = src.dclass;
  out->flex = src.flex;
  out->active = src.active;
  out->solref = src.solref;
  out->solimp = src.solimp;
  return out;
}

bool operator==(const EqualityFlex& a, const EqualityFlex& b) {
  return a.name == b.name &&
         a.dclass == b.dclass &&
         a.flex == b.flex &&
         a.active == b.active &&
         a.solref == b.solref &&
         a.solimp == b.solimp;
}

std::unique_ptr<EqualityJoint> Clone(const EqualityJoint& src) {
  auto out = std::make_unique<EqualityJoint>();
  out->loc = src.loc;
  out->name = src.name;
  out->dclass = src.dclass;
  out->joint1 = src.joint1;
  out->joint2 = src.joint2;
  out->polycoef = src.polycoef;
  out->active = src.active;
  out->solref = src.solref;
  out->solimp = src.solimp;
  return out;
}

bool operator==(const EqualityJoint& a, const EqualityJoint& b) {
  return a.name == b.name &&
         a.dclass == b.dclass &&
         a.joint1 == b.joint1 &&
         a.joint2 == b.joint2 &&
         a.polycoef == b.polycoef &&
         a.active == b.active &&
         a.solref == b.solref &&
         a.solimp == b.solimp;
}

std::unique_ptr<EqualityTendon> Clone(const EqualityTendon& src) {
  auto out = std::make_unique<EqualityTendon>();
  out->loc = src.loc;
  out->name = src.name;
  out->dclass = src.dclass;
  out->tendon1 = src.tendon1;
  out->tendon2 = src.tendon2;
  out->polycoef = src.polycoef;
  out->active = src.active;
  out->solref = src.solref;
  out->solimp = src.solimp;
  return out;
}

bool operator==(const EqualityTendon& a, const EqualityTendon& b) {
  return a.name == b.name &&
         a.dclass == b.dclass &&
         a.tendon1 == b.tendon1 &&
         a.tendon2 == b.tendon2 &&
         a.polycoef == b.polycoef &&
         a.active == b.active &&
         a.solref == b.solref &&
         a.solimp == b.solimp;
}

std::unique_ptr<Exclude> Clone(const Exclude& src) {
  auto out = std::make_unique<Exclude>();
  out->loc = src.loc;
  out->name = src.name;
  out->body1 = src.body1;
  out->body2 = src.body2;
  return out;
}

bool operator==(const Exclude& a, const Exclude& b) {
  return a.name == b.name &&
         a.body1 == b.body1 &&
         a.body2 == b.body2;
}

std::unique_ptr<Extension> Clone(const Extension& src) {
  auto out = std::make_unique<Extension>();
  out->loc = src.loc;
  out->pluginDefs = ps::PtrVecClone(src.pluginDefs);
  return out;
}

bool operator==(const Extension& a, const Extension& b) {
  return ps::PtrVecEq(a.pluginDefs, b.pluginDefs);
}

std::unique_ptr<Fixed> Clone(const Fixed& src) {
  auto out = std::make_unique<Fixed>();
  out->loc = src.loc;
  out->name = src.name;
  out->dclass = src.dclass;
  out->group = src.group;
  out->limited = src.limited;
  out->actuatorfrclimited = src.actuatorfrclimited;
  out->range = src.range;
  out->actuatorfrcrange = src.actuatorfrcrange;
  out->solreflimit = src.solreflimit;
  out->solimplimit = src.solimplimit;
  out->solreffriction = src.solreffriction;
  out->solimpfriction = src.solimpfriction;
  out->frictionloss = src.frictionloss;
  out->springlength = src.springlength;
  out->margin = src.margin;
  out->stiffness = src.stiffness;
  out->damping = src.damping;
  out->armature = src.armature;
  out->user = src.user;
  out->fixedJoints = ps::PtrVecClone(src.fixedJoints);
  return out;
}

bool operator==(const Fixed& a, const Fixed& b) {
  return a.name == b.name &&
         a.dclass == b.dclass &&
         a.group == b.group &&
         a.limited == b.limited &&
         a.actuatorfrclimited == b.actuatorfrclimited &&
         a.range == b.range &&
         a.actuatorfrcrange == b.actuatorfrcrange &&
         a.solreflimit == b.solreflimit &&
         a.solimplimit == b.solimplimit &&
         a.solreffriction == b.solreffriction &&
         a.solimpfriction == b.solimpfriction &&
         a.frictionloss == b.frictionloss &&
         a.springlength == b.springlength &&
         a.margin == b.margin &&
         a.stiffness == b.stiffness &&
         a.damping == b.damping &&
         a.armature == b.armature &&
         a.user == b.user &&
         ps::PtrVecEq(a.fixedJoints, b.fixedJoints);
}

std::unique_ptr<FixedJoint> Clone(const FixedJoint& src) {
  auto out = std::make_unique<FixedJoint>();
  out->loc = src.loc;
  out->joint = src.joint;
  out->coef = src.coef;
  return out;
}

bool operator==(const FixedJoint& a, const FixedJoint& b) {
  return a.joint == b.joint &&
         a.coef == b.coef;
}

std::unique_ptr<Flag> Clone(const Flag& src) {
  auto out = std::make_unique<Flag>();
  out->loc = src.loc;
  out->constraint = src.constraint;
  out->equality = src.equality;
  out->frictionloss = src.frictionloss;
  out->limit = src.limit;
  out->contact = src.contact;
  out->spring = src.spring;
  out->damper = src.damper;
  out->gravity = src.gravity;
  out->clampctrl = src.clampctrl;
  out->warmstart = src.warmstart;
  out->filterparent = src.filterparent;
  out->actuation = src.actuation;
  out->refsafe = src.refsafe;
  out->sensor = src.sensor;
  out->midphase = src.midphase;
  out->eulerdamp = src.eulerdamp;
  out->autoreset = src.autoreset;
  out->nativeccd = src.nativeccd;
  out->island = src.island;
  out->override_ = src.override_;
  out->energy = src.energy;
  out->fwdinv = src.fwdinv;
  out->invdiscrete = src.invdiscrete;
  out->multiccd = src.multiccd;
  out->sleep = src.sleep;
  out->diagexact = src.diagexact;
  return out;
}

bool operator==(const Flag& a, const Flag& b) {
  return a.constraint == b.constraint &&
         a.equality == b.equality &&
         a.frictionloss == b.frictionloss &&
         a.limit == b.limit &&
         a.contact == b.contact &&
         a.spring == b.spring &&
         a.damper == b.damper &&
         a.gravity == b.gravity &&
         a.clampctrl == b.clampctrl &&
         a.warmstart == b.warmstart &&
         a.filterparent == b.filterparent &&
         a.actuation == b.actuation &&
         a.refsafe == b.refsafe &&
         a.sensor == b.sensor &&
         a.midphase == b.midphase &&
         a.eulerdamp == b.eulerdamp &&
         a.autoreset == b.autoreset &&
         a.nativeccd == b.nativeccd &&
         a.island == b.island &&
         a.override_ == b.override_ &&
         a.energy == b.energy &&
         a.fwdinv == b.fwdinv &&
         a.invdiscrete == b.invdiscrete &&
         a.multiccd == b.multiccd &&
         a.sleep == b.sleep &&
         a.diagexact == b.diagexact;
}

std::unique_ptr<Flex> Clone(const Flex& src) {
  auto out = std::make_unique<Flex>();
  out->loc = src.loc;
  out->name = src.name;
  out->group = src.group;
  out->dim = src.dim;
  out->radius = src.radius;
  out->material = src.material;
  out->rgba = src.rgba;
  out->flatskin = src.flatskin;
  out->body = src.body;
  out->vertex = src.vertex;
  out->element = src.element;
  out->texcoord = src.texcoord;
  out->elemtexcoord = src.elemtexcoord;
  out->node = src.node;
  out->cellcount = src.cellcount;
  out->dof = src.dof;
  out->flexContacts = ps::PtrVecClone(src.flexContacts);
  out->flexEdges = ps::PtrVecClone(src.flexEdges);
  out->flexElasticitys = ps::PtrVecClone(src.flexElasticitys);
  return out;
}

bool operator==(const Flex& a, const Flex& b) {
  return a.name == b.name &&
         a.group == b.group &&
         a.dim == b.dim &&
         a.radius == b.radius &&
         a.material == b.material &&
         a.rgba == b.rgba &&
         a.flatskin == b.flatskin &&
         a.body == b.body &&
         a.vertex == b.vertex &&
         a.element == b.element &&
         a.texcoord == b.texcoord &&
         a.elemtexcoord == b.elemtexcoord &&
         a.node == b.node &&
         a.cellcount == b.cellcount &&
         a.dof == b.dof &&
         ps::PtrVecEq(a.flexContacts, b.flexContacts) &&
         ps::PtrVecEq(a.flexEdges, b.flexEdges) &&
         ps::PtrVecEq(a.flexElasticitys, b.flexElasticitys);
}

std::unique_ptr<FlexContact> Clone(const FlexContact& src) {
  auto out = std::make_unique<FlexContact>();
  out->loc = src.loc;
  out->contype = src.contype;
  out->conaffinity = src.conaffinity;
  out->condim = src.condim;
  out->priority = src.priority;
  out->friction = src.friction;
  out->solmix = src.solmix;
  out->solref = src.solref;
  out->solimp = src.solimp;
  out->margin = src.margin;
  out->gap = src.gap;
  out->internal = src.internal;
  out->selfcollide = src.selfcollide;
  out->activelayers = src.activelayers;
  out->passive = src.passive;
  return out;
}

bool operator==(const FlexContact& a, const FlexContact& b) {
  return a.contype == b.contype &&
         a.conaffinity == b.conaffinity &&
         a.condim == b.condim &&
         a.priority == b.priority &&
         a.friction == b.friction &&
         a.solmix == b.solmix &&
         a.solref == b.solref &&
         a.solimp == b.solimp &&
         a.margin == b.margin &&
         a.gap == b.gap &&
         a.internal == b.internal &&
         a.selfcollide == b.selfcollide &&
         a.activelayers == b.activelayers &&
         a.passive == b.passive;
}

std::unique_ptr<FlexEdge> Clone(const FlexEdge& src) {
  auto out = std::make_unique<FlexEdge>();
  out->loc = src.loc;
  out->stiffness = src.stiffness;
  out->damping = src.damping;
  return out;
}

bool operator==(const FlexEdge& a, const FlexEdge& b) {
  return a.stiffness == b.stiffness &&
         a.damping == b.damping;
}

std::unique_ptr<FlexElasticity> Clone(const FlexElasticity& src) {
  auto out = std::make_unique<FlexElasticity>();
  out->loc = src.loc;
  out->young = src.young;
  out->poisson = src.poisson;
  out->damping = src.damping;
  out->thickness = src.thickness;
  out->elastic2d = src.elastic2d;
  return out;
}

bool operator==(const FlexElasticity& a, const FlexElasticity& b) {
  return a.young == b.young &&
         a.poisson == b.poisson &&
         a.damping == b.damping &&
         a.thickness == b.thickness &&
         a.elastic2d == b.elastic2d;
}

std::unique_ptr<Flexcomp> Clone(const Flexcomp& src) {
  auto out = std::make_unique<Flexcomp>();
  out->loc = src.loc;
  out->name = src.name;
  out->type = src.type;
  out->group = src.group;
  out->dim = src.dim;
  out->dof = src.dof;
  out->count = src.count;
  out->cellcount = src.cellcount;
  out->spacing = src.spacing;
  out->radius = src.radius;
  out->rigid = src.rigid;
  out->mass = src.mass;
  out->inertiabox = src.inertiabox;
  out->scale = src.scale;
  out->file = src.file;
  out->point = src.point;
  out->element = src.element;
  out->texcoord = src.texcoord;
  out->material = src.material;
  out->rgba = src.rgba;
  out->flatskin = src.flatskin;
  out->pos = src.pos;
  out->quat = src.quat;
  out->origin = src.origin;
  out->flexcompEdges = ps::PtrVecClone(src.flexcompEdges);
  out->flexElasticitys = ps::PtrVecClone(src.flexElasticitys);
  out->flexContacts = ps::PtrVecClone(src.flexContacts);
  out->flexcompPins = ps::PtrVecClone(src.flexcompPins);
  out->plugin = ps::PtrVecClone(src.plugin);
  return out;
}

bool operator==(const Flexcomp& a, const Flexcomp& b) {
  return a.name == b.name &&
         a.type == b.type &&
         a.group == b.group &&
         a.dim == b.dim &&
         a.dof == b.dof &&
         a.count == b.count &&
         a.cellcount == b.cellcount &&
         a.spacing == b.spacing &&
         a.radius == b.radius &&
         a.rigid == b.rigid &&
         a.mass == b.mass &&
         a.inertiabox == b.inertiabox &&
         a.scale == b.scale &&
         a.file == b.file &&
         a.point == b.point &&
         a.element == b.element &&
         a.texcoord == b.texcoord &&
         a.material == b.material &&
         a.rgba == b.rgba &&
         a.flatskin == b.flatskin &&
         a.pos == b.pos &&
         a.quat == b.quat &&
         a.origin == b.origin &&
         ps::PtrVecEq(a.flexcompEdges, b.flexcompEdges) &&
         ps::PtrVecEq(a.flexElasticitys, b.flexElasticitys) &&
         ps::PtrVecEq(a.flexContacts, b.flexContacts) &&
         ps::PtrVecEq(a.flexcompPins, b.flexcompPins) &&
         ps::PtrVecEq(a.plugin, b.plugin);
}

std::unique_ptr<FlexcompEdge> Clone(const FlexcompEdge& src) {
  auto out = std::make_unique<FlexcompEdge>();
  out->loc = src.loc;
  out->equality = src.equality;
  out->solref = src.solref;
  out->solimp = src.solimp;
  out->stiffness = src.stiffness;
  out->damping = src.damping;
  return out;
}

bool operator==(const FlexcompEdge& a, const FlexcompEdge& b) {
  return a.equality == b.equality &&
         a.solref == b.solref &&
         a.solimp == b.solimp &&
         a.stiffness == b.stiffness &&
         a.damping == b.damping;
}

std::unique_ptr<FlexcompPin> Clone(const FlexcompPin& src) {
  auto out = std::make_unique<FlexcompPin>();
  out->loc = src.loc;
  out->id = src.id;
  out->range = src.range;
  out->grid = src.grid;
  out->gridrange = src.gridrange;
  return out;
}

bool operator==(const FlexcompPin& a, const FlexcompPin& b) {
  return a.id == b.id &&
         a.range == b.range &&
         a.grid == b.grid &&
         a.gridrange == b.gridrange;
}

std::unique_ptr<Flexstrain> Clone(const Flexstrain& src) {
  auto out = std::make_unique<Flexstrain>();
  out->loc = src.loc;
  out->name = src.name;
  out->dclass = src.dclass;
  out->flex = src.flex;
  out->cell = src.cell;
  out->active = src.active;
  out->solref = src.solref;
  out->solimp = src.solimp;
  return out;
}

bool operator==(const Flexstrain& a, const Flexstrain& b) {
  return a.name == b.name &&
         a.dclass == b.dclass &&
         a.flex == b.flex &&
         a.cell == b.cell &&
         a.active == b.active &&
         a.solref == b.solref &&
         a.solimp == b.solimp;
}

std::unique_ptr<Flexvert> Clone(const Flexvert& src) {
  auto out = std::make_unique<Flexvert>();
  out->loc = src.loc;
  out->name = src.name;
  out->dclass = src.dclass;
  out->flex = src.flex;
  out->active = src.active;
  out->solref = src.solref;
  out->solimp = src.solimp;
  return out;
}

bool operator==(const Flexvert& a, const Flexvert& b) {
  return a.name == b.name &&
         a.dclass == b.dclass &&
         a.flex == b.flex &&
         a.active == b.active &&
         a.solref == b.solref &&
         a.solimp == b.solimp;
}

std::unique_ptr<Force> Clone(const Force& src) {
  auto out = std::make_unique<Force>();
  out->loc = src.loc;
  out->name = src.name;
  out->site = src.site;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Force& a, const Force& b) {
  return a.name == b.name &&
         a.site == b.site &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Frame> Clone(const Frame& src) {
  auto out = std::make_unique<Frame>();
  out->loc = src.loc;
  out->pos = src.pos;
  out->quat = src.quat;
  out->name = src.name;
  out->dclass = src.dclass;
  out->inertial = ps::PtrVecClone(src.inertial);
  for (const auto& item : src.subtree)
    out->subtree.push_back(Clone(item));
  return out;
}

bool operator==(const Frame& a, const Frame& b) {
  return a.pos == b.pos &&
         a.quat == b.quat &&
         a.name == b.name &&
         a.dclass == b.dclass &&
         ps::PtrVecEq(a.inertial, b.inertial) &&
         a.subtree == b.subtree;
}

std::unique_ptr<Frameangacc> Clone(const Frameangacc& src) {
  auto out = std::make_unique<Frameangacc>();
  out->loc = src.loc;
  out->name = src.name;
  out->objtype = src.objtype;
  out->objname = src.objname;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Frameangacc& a, const Frameangacc& b) {
  return a.name == b.name &&
         a.objtype == b.objtype &&
         a.objname == b.objname &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Frameangvel> Clone(const Frameangvel& src) {
  auto out = std::make_unique<Frameangvel>();
  out->loc = src.loc;
  out->name = src.name;
  out->objtype = src.objtype;
  out->objname = src.objname;
  out->reftype = src.reftype;
  out->refname = src.refname;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Frameangvel& a, const Frameangvel& b) {
  return a.name == b.name &&
         a.objtype == b.objtype &&
         a.objname == b.objname &&
         a.reftype == b.reftype &&
         a.refname == b.refname &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Framelinacc> Clone(const Framelinacc& src) {
  auto out = std::make_unique<Framelinacc>();
  out->loc = src.loc;
  out->name = src.name;
  out->objtype = src.objtype;
  out->objname = src.objname;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Framelinacc& a, const Framelinacc& b) {
  return a.name == b.name &&
         a.objtype == b.objtype &&
         a.objname == b.objname &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Framelinvel> Clone(const Framelinvel& src) {
  auto out = std::make_unique<Framelinvel>();
  out->loc = src.loc;
  out->name = src.name;
  out->objtype = src.objtype;
  out->objname = src.objname;
  out->reftype = src.reftype;
  out->refname = src.refname;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Framelinvel& a, const Framelinvel& b) {
  return a.name == b.name &&
         a.objtype == b.objtype &&
         a.objname == b.objname &&
         a.reftype == b.reftype &&
         a.refname == b.refname &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Framepos> Clone(const Framepos& src) {
  auto out = std::make_unique<Framepos>();
  out->loc = src.loc;
  out->name = src.name;
  out->objtype = src.objtype;
  out->objname = src.objname;
  out->reftype = src.reftype;
  out->refname = src.refname;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Framepos& a, const Framepos& b) {
  return a.name == b.name &&
         a.objtype == b.objtype &&
         a.objname == b.objname &&
         a.reftype == b.reftype &&
         a.refname == b.refname &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Framequat> Clone(const Framequat& src) {
  auto out = std::make_unique<Framequat>();
  out->loc = src.loc;
  out->name = src.name;
  out->objtype = src.objtype;
  out->objname = src.objname;
  out->reftype = src.reftype;
  out->refname = src.refname;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Framequat& a, const Framequat& b) {
  return a.name == b.name &&
         a.objtype == b.objtype &&
         a.objname == b.objname &&
         a.reftype == b.reftype &&
         a.refname == b.refname &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Framexaxis> Clone(const Framexaxis& src) {
  auto out = std::make_unique<Framexaxis>();
  out->loc = src.loc;
  out->name = src.name;
  out->objtype = src.objtype;
  out->objname = src.objname;
  out->reftype = src.reftype;
  out->refname = src.refname;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Framexaxis& a, const Framexaxis& b) {
  return a.name == b.name &&
         a.objtype == b.objtype &&
         a.objname == b.objname &&
         a.reftype == b.reftype &&
         a.refname == b.refname &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Frameyaxis> Clone(const Frameyaxis& src) {
  auto out = std::make_unique<Frameyaxis>();
  out->loc = src.loc;
  out->name = src.name;
  out->objtype = src.objtype;
  out->objname = src.objname;
  out->reftype = src.reftype;
  out->refname = src.refname;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Frameyaxis& a, const Frameyaxis& b) {
  return a.name == b.name &&
         a.objtype == b.objtype &&
         a.objname == b.objname &&
         a.reftype == b.reftype &&
         a.refname == b.refname &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Framezaxis> Clone(const Framezaxis& src) {
  auto out = std::make_unique<Framezaxis>();
  out->loc = src.loc;
  out->name = src.name;
  out->objtype = src.objtype;
  out->objname = src.objname;
  out->reftype = src.reftype;
  out->refname = src.refname;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Framezaxis& a, const Framezaxis& b) {
  return a.name == b.name &&
         a.objtype == b.objtype &&
         a.objname == b.objname &&
         a.reftype == b.reftype &&
         a.refname == b.refname &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<FreeJoint> Clone(const FreeJoint& src) {
  auto out = std::make_unique<FreeJoint>();
  out->loc = src.loc;
  out->name = src.name;
  out->group = src.group;
  out->align = src.align;
  return out;
}

bool operator==(const FreeJoint& a, const FreeJoint& b) {
  return a.name == b.name &&
         a.group == b.group &&
         a.align == b.align;
}

std::unique_ptr<Fromto> Clone(const Fromto& src) {
  auto out = std::make_unique<Fromto>();
  out->loc = src.loc;
  out->name = src.name;
  out->geom1 = src.geom1;
  out->geom2 = src.geom2;
  out->body1 = src.body1;
  out->body2 = src.body2;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Fromto& a, const Fromto& b) {
  return a.name == b.name &&
         a.geom1 == b.geom1 &&
         a.geom2 == b.geom2 &&
         a.body1 == b.body1 &&
         a.body2 == b.body2 &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Geom> Clone(const Geom& src) {
  auto out = std::make_unique<Geom>();
  out->loc = src.loc;
  out->pos = src.pos;
  out->quat = src.quat;
  out->name = src.name;
  out->dclass = src.dclass;
  out->type = src.type;
  out->contype = src.contype;
  out->conaffinity = src.conaffinity;
  out->condim = src.condim;
  out->group = src.group;
  out->priority = src.priority;
  out->size = src.size;
  out->material = src.material;
  out->friction = src.friction;
  out->mass = src.mass;
  out->density = src.density;
  out->shellinertia = src.shellinertia;
  out->solmix = src.solmix;
  out->solref = src.solref;
  out->solimp = src.solimp;
  out->margin = src.margin;
  out->gap = src.gap;
  out->surfacevel = src.surfacevel;
  out->hfield = src.hfield;
  out->mesh = src.mesh;
  out->fitscale = src.fitscale;
  out->rgba = src.rgba;
  out->fluidshape = src.fluidshape;
  out->fluidcoef = src.fluidcoef;
  out->user = src.user;
  out->shape = src.shape;
  out->plugin = ps::PtrVecClone(src.plugin);
  return out;
}

bool operator==(const Geom& a, const Geom& b) {
  return a.pos == b.pos &&
         a.quat == b.quat &&
         a.name == b.name &&
         a.dclass == b.dclass &&
         a.type == b.type &&
         a.contype == b.contype &&
         a.conaffinity == b.conaffinity &&
         a.condim == b.condim &&
         a.group == b.group &&
         a.priority == b.priority &&
         a.size == b.size &&
         a.material == b.material &&
         a.friction == b.friction &&
         a.mass == b.mass &&
         a.density == b.density &&
         a.shellinertia == b.shellinertia &&
         a.solmix == b.solmix &&
         a.solref == b.solref &&
         a.solimp == b.solimp &&
         a.margin == b.margin &&
         a.gap == b.gap &&
         a.surfacevel == b.surfacevel &&
         a.hfield == b.hfield &&
         a.mesh == b.mesh &&
         a.fitscale == b.fitscale &&
         a.rgba == b.rgba &&
         a.fluidshape == b.fluidshape &&
         a.fluidcoef == b.fluidcoef &&
         a.user == b.user &&
         a.shape == b.shape &&
         ps::PtrVecEq(a.plugin, b.plugin);
}

std::unique_ptr<Gyro> Clone(const Gyro& src) {
  auto out = std::make_unique<Gyro>();
  out->loc = src.loc;
  out->name = src.name;
  out->site = src.site;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Gyro& a, const Gyro& b) {
  return a.name == b.name &&
         a.site == b.site &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Hfield> Clone(const Hfield& src) {
  auto out = std::make_unique<Hfield>();
  out->loc = src.loc;
  out->name = src.name;
  out->content_type = src.content_type;
  out->file = src.file;
  out->nrow = src.nrow;
  out->ncol = src.ncol;
  out->size = src.size;
  out->elevation = src.elevation;
  return out;
}

bool operator==(const Hfield& a, const Hfield& b) {
  return a.name == b.name &&
         a.content_type == b.content_type &&
         a.file == b.file &&
         a.nrow == b.nrow &&
         a.ncol == b.ncol &&
         a.size == b.size &&
         a.elevation == b.elevation;
}

std::unique_ptr<Inertial> Clone(const Inertial& src) {
  auto out = std::make_unique<Inertial>();
  out->loc = src.loc;
  out->pos = src.pos;
  out->mass = src.mass;
  out->iquat = src.iquat;
  out->diaginertia = src.diaginertia;
  return out;
}

bool operator==(const Inertial& a, const Inertial& b) {
  return a.pos == b.pos &&
         a.mass == b.mass &&
         a.iquat == b.iquat &&
         a.diaginertia == b.diaginertia;
}

std::unique_ptr<Insidesite> Clone(const Insidesite& src) {
  auto out = std::make_unique<Insidesite>();
  out->loc = src.loc;
  out->name = src.name;
  out->site = src.site;
  out->objtype = src.objtype;
  out->objname = src.objname;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Insidesite& a, const Insidesite& b) {
  return a.name == b.name &&
         a.site == b.site &&
         a.objtype == b.objtype &&
         a.objname == b.objname &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<IntVelocity> Clone(const IntVelocity& src) {
  auto out = std::make_unique<IntVelocity>();
  out->loc = src.loc;
  out->name = src.name;
  out->dclass = src.dclass;
  out->group = src.group;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->ctrllimited = src.ctrllimited;
  out->forcelimited = src.forcelimited;
  out->actlimited = src.actlimited;
  out->ctrlrange = src.ctrlrange;
  out->forcerange = src.forcerange;
  out->actrange = src.actrange;
  out->inheritrange = src.inheritrange;
  out->lengthrange = src.lengthrange;
  out->gear = src.gear;
  out->damping = src.damping;
  out->armature = src.armature;
  out->cranklength = src.cranklength;
  out->user = src.user;
  out->joint = src.joint;
  out->jointinparent = src.jointinparent;
  out->tendon = src.tendon;
  out->slidersite = src.slidersite;
  out->cranksite = src.cranksite;
  out->site = src.site;
  out->refsite = src.refsite;
  out->kp = src.kp;
  out->kv = src.kv;
  out->dampratio = src.dampratio;
  return out;
}

bool operator==(const IntVelocity& a, const IntVelocity& b) {
  return a.name == b.name &&
         a.dclass == b.dclass &&
         a.group == b.group &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.ctrllimited == b.ctrllimited &&
         a.forcelimited == b.forcelimited &&
         a.actlimited == b.actlimited &&
         a.ctrlrange == b.ctrlrange &&
         a.forcerange == b.forcerange &&
         a.actrange == b.actrange &&
         a.inheritrange == b.inheritrange &&
         a.lengthrange == b.lengthrange &&
         a.gear == b.gear &&
         a.damping == b.damping &&
         a.armature == b.armature &&
         a.cranklength == b.cranklength &&
         a.user == b.user &&
         a.joint == b.joint &&
         a.jointinparent == b.jointinparent &&
         a.tendon == b.tendon &&
         a.slidersite == b.slidersite &&
         a.cranksite == b.cranksite &&
         a.site == b.site &&
         a.refsite == b.refsite &&
         a.kp == b.kp &&
         a.kv == b.kv &&
         a.dampratio == b.dampratio;
}

std::unique_ptr<Joint> Clone(const Joint& src) {
  auto out = std::make_unique<Joint>();
  out->loc = src.loc;
  out->name = src.name;
  out->dclass = src.dclass;
  out->type = src.type;
  out->group = src.group;
  out->pos = src.pos;
  out->axis = src.axis;
  out->springdamper = src.springdamper;
  out->limited = src.limited;
  out->actuatorfrclimited = src.actuatorfrclimited;
  out->solreflimit = src.solreflimit;
  out->solimplimit = src.solimplimit;
  out->solreffriction = src.solreffriction;
  out->solimpfriction = src.solimpfriction;
  out->stiffness = src.stiffness;
  out->range = src.range;
  out->actuatorfrcrange = src.actuatorfrcrange;
  out->actuatorgravcomp = src.actuatorgravcomp;
  out->margin = src.margin;
  out->ref = src.ref;
  out->springref = src.springref;
  out->armature = src.armature;
  out->damping = src.damping;
  out->frictionloss = src.frictionloss;
  out->user = src.user;
  return out;
}

bool operator==(const Joint& a, const Joint& b) {
  return a.name == b.name &&
         a.dclass == b.dclass &&
         a.type == b.type &&
         a.group == b.group &&
         a.pos == b.pos &&
         a.axis == b.axis &&
         a.springdamper == b.springdamper &&
         a.limited == b.limited &&
         a.actuatorfrclimited == b.actuatorfrclimited &&
         a.solreflimit == b.solreflimit &&
         a.solimplimit == b.solimplimit &&
         a.solreffriction == b.solreffriction &&
         a.solimpfriction == b.solimpfriction &&
         a.stiffness == b.stiffness &&
         a.range == b.range &&
         a.actuatorfrcrange == b.actuatorfrcrange &&
         a.actuatorgravcomp == b.actuatorgravcomp &&
         a.margin == b.margin &&
         a.ref == b.ref &&
         a.springref == b.springref &&
         a.armature == b.armature &&
         a.damping == b.damping &&
         a.frictionloss == b.frictionloss &&
         a.user == b.user;
}

std::unique_ptr<Jointactuatorfrc> Clone(const Jointactuatorfrc& src) {
  auto out = std::make_unique<Jointactuatorfrc>();
  out->loc = src.loc;
  out->name = src.name;
  out->joint = src.joint;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Jointactuatorfrc& a, const Jointactuatorfrc& b) {
  return a.name == b.name &&
         a.joint == b.joint &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Jointlimitfrc> Clone(const Jointlimitfrc& src) {
  auto out = std::make_unique<Jointlimitfrc>();
  out->loc = src.loc;
  out->name = src.name;
  out->joint = src.joint;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Jointlimitfrc& a, const Jointlimitfrc& b) {
  return a.name == b.name &&
         a.joint == b.joint &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Jointlimitpos> Clone(const Jointlimitpos& src) {
  auto out = std::make_unique<Jointlimitpos>();
  out->loc = src.loc;
  out->name = src.name;
  out->joint = src.joint;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Jointlimitpos& a, const Jointlimitpos& b) {
  return a.name == b.name &&
         a.joint == b.joint &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Jointlimitvel> Clone(const Jointlimitvel& src) {
  auto out = std::make_unique<Jointlimitvel>();
  out->loc = src.loc;
  out->name = src.name;
  out->joint = src.joint;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Jointlimitvel& a, const Jointlimitvel& b) {
  return a.name == b.name &&
         a.joint == b.joint &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Jointpos> Clone(const Jointpos& src) {
  auto out = std::make_unique<Jointpos>();
  out->loc = src.loc;
  out->name = src.name;
  out->joint = src.joint;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Jointpos& a, const Jointpos& b) {
  return a.name == b.name &&
         a.joint == b.joint &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Jointvel> Clone(const Jointvel& src) {
  auto out = std::make_unique<Jointvel>();
  out->loc = src.loc;
  out->name = src.name;
  out->joint = src.joint;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Jointvel& a, const Jointvel& b) {
  return a.name == b.name &&
         a.joint == b.joint &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Key> Clone(const Key& src) {
  auto out = std::make_unique<Key>();
  out->loc = src.loc;
  out->name = src.name;
  out->time = src.time;
  out->qpos = src.qpos;
  out->qvel = src.qvel;
  out->act = src.act;
  out->mpos = src.mpos;
  out->mquat = src.mquat;
  out->ctrl = src.ctrl;
  return out;
}

bool operator==(const Key& a, const Key& b) {
  return a.name == b.name &&
         a.time == b.time &&
         a.qpos == b.qpos &&
         a.qvel == b.qvel &&
         a.act == b.act &&
         a.mpos == b.mpos &&
         a.mquat == b.mquat &&
         a.ctrl == b.ctrl;
}

std::unique_ptr<Keyframe> Clone(const Keyframe& src) {
  auto out = std::make_unique<Keyframe>();
  out->loc = src.loc;
  out->keys = ps::PtrVecClone(src.keys);
  return out;
}

bool operator==(const Keyframe& a, const Keyframe& b) {
  return ps::PtrVecEq(a.keys, b.keys);
}

std::unique_ptr<LengthRange> Clone(const LengthRange& src) {
  auto out = std::make_unique<LengthRange>();
  out->loc = src.loc;
  out->mode = src.mode;
  out->useexisting = src.useexisting;
  out->uselimit = src.uselimit;
  out->accel = src.accel;
  out->maxforce = src.maxforce;
  out->timeconst = src.timeconst;
  out->timestep = src.timestep;
  out->inttotal = src.inttotal;
  out->interval = src.interval;
  out->tolrange = src.tolrange;
  return out;
}

bool operator==(const LengthRange& a, const LengthRange& b) {
  return a.mode == b.mode &&
         a.useexisting == b.useexisting &&
         a.uselimit == b.uselimit &&
         a.accel == b.accel &&
         a.maxforce == b.maxforce &&
         a.timeconst == b.timeconst &&
         a.timestep == b.timestep &&
         a.inttotal == b.inttotal &&
         a.interval == b.interval &&
         a.tolrange == b.tolrange;
}

std::unique_ptr<Light> Clone(const Light& src) {
  auto out = std::make_unique<Light>();
  out->loc = src.loc;
  out->name = src.name;
  out->dclass = src.dclass;
  out->type = src.type;
  out->castshadow = src.castshadow;
  out->active = src.active;
  out->pos = src.pos;
  out->dir = src.dir;
  out->bulbradius = src.bulbradius;
  out->intensity = src.intensity;
  out->range = src.range;
  out->attenuation = src.attenuation;
  out->cutoff = src.cutoff;
  out->exponent = src.exponent;
  out->ambient = src.ambient;
  out->diffuse = src.diffuse;
  out->specular = src.specular;
  out->mode = src.mode;
  out->target = src.target;
  out->texture = src.texture;
  return out;
}

bool operator==(const Light& a, const Light& b) {
  return a.name == b.name &&
         a.dclass == b.dclass &&
         a.type == b.type &&
         a.castshadow == b.castshadow &&
         a.active == b.active &&
         a.pos == b.pos &&
         a.dir == b.dir &&
         a.bulbradius == b.bulbradius &&
         a.intensity == b.intensity &&
         a.range == b.range &&
         a.attenuation == b.attenuation &&
         a.cutoff == b.cutoff &&
         a.exponent == b.exponent &&
         a.ambient == b.ambient &&
         a.diffuse == b.diffuse &&
         a.specular == b.specular &&
         a.mode == b.mode &&
         a.target == b.target &&
         a.texture == b.texture;
}

std::unique_ptr<Magnetometer> Clone(const Magnetometer& src) {
  auto out = std::make_unique<Magnetometer>();
  out->loc = src.loc;
  out->name = src.name;
  out->site = src.site;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Magnetometer& a, const Magnetometer& b) {
  return a.name == b.name &&
         a.site == b.site &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Material> Clone(const Material& src) {
  auto out = std::make_unique<Material>();
  out->loc = src.loc;
  out->name = src.name;
  out->dclass = src.dclass;
  out->texrepeat = src.texrepeat;
  out->texuniform = src.texuniform;
  out->emission = src.emission;
  out->specular = src.specular;
  out->shininess = src.shininess;
  out->reflectance = src.reflectance;
  out->metallic = src.metallic;
  out->roughness = src.roughness;
  out->rgba = src.rgba;
  out->layers = ps::PtrVecClone(src.layers);
  return out;
}

bool operator==(const Material& a, const Material& b) {
  return a.name == b.name &&
         a.dclass == b.dclass &&
         a.texrepeat == b.texrepeat &&
         a.texuniform == b.texuniform &&
         a.emission == b.emission &&
         a.specular == b.specular &&
         a.shininess == b.shininess &&
         a.reflectance == b.reflectance &&
         a.metallic == b.metallic &&
         a.roughness == b.roughness &&
         a.rgba == b.rgba &&
         ps::PtrVecEq(a.layers, b.layers);
}

std::unique_ptr<MaterialLayer> Clone(const MaterialLayer& src) {
  auto out = std::make_unique<MaterialLayer>();
  out->loc = src.loc;
  out->texture = src.texture;
  out->role = src.role;
  return out;
}

bool operator==(const MaterialLayer& a, const MaterialLayer& b) {
  return a.texture == b.texture &&
         a.role == b.role;
}

std::unique_ptr<Mesh> Clone(const Mesh& src) {
  auto out = std::make_unique<Mesh>();
  out->loc = src.loc;
  out->name = src.name;
  out->dclass = src.dclass;
  out->content_type = src.content_type;
  out->file = src.file;
  out->vertex = src.vertex;
  out->normal = src.normal;
  out->texcoord = src.texcoord;
  out->face = src.face;
  out->refpos = src.refpos;
  out->refquat = src.refquat;
  out->scale = src.scale;
  out->smoothnormal = src.smoothnormal;
  out->maxhullvert = src.maxhullvert;
  out->inertia = src.inertia;
  out->builtin = src.builtin;
  out->params = src.params;
  out->material = src.material;
  out->plugin = ps::PtrVecClone(src.plugin);
  return out;
}

bool operator==(const Mesh& a, const Mesh& b) {
  return a.name == b.name &&
         a.dclass == b.dclass &&
         a.content_type == b.content_type &&
         a.file == b.file &&
         a.vertex == b.vertex &&
         a.normal == b.normal &&
         a.texcoord == b.texcoord &&
         a.face == b.face &&
         a.refpos == b.refpos &&
         a.refquat == b.refquat &&
         a.scale == b.scale &&
         a.smoothnormal == b.smoothnormal &&
         a.maxhullvert == b.maxhullvert &&
         a.inertia == b.inertia &&
         a.builtin == b.builtin &&
         a.params == b.params &&
         a.material == b.material &&
         ps::PtrVecEq(a.plugin, b.plugin);
}

std::unique_ptr<Model> Clone(const Model& src) {
  auto out = std::make_unique<Model>();
  out->loc = src.loc;
  out->model = src.model;
  out->compilers = ps::PtrVecClone(src.compilers);
  out->options = ps::PtrVecClone(src.options);
  out->sizes = ps::PtrVecClone(src.sizes);
  out->visuals = ps::PtrVecClone(src.visuals);
  out->statistics = ps::PtrVecClone(src.statistics);
  out->defaults = ps::PtrVecClone(src.defaults);
  out->extensions = ps::PtrVecClone(src.extensions);
  out->customs = ps::PtrVecClone(src.customs);
  out->assets = ps::PtrVecClone(src.assets);
  out->worldbody = ps::PtrVecClone(src.worldbody);
  out->deformables = ps::PtrVecClone(src.deformables);
  out->contacts = ps::PtrVecClone(src.contacts);
  out->equalitys = ps::PtrVecClone(src.equalitys);
  out->tendons = ps::PtrVecClone(src.tendons);
  out->actuators = ps::PtrVecClone(src.actuators);
  out->sensors = ps::PtrVecClone(src.sensors);
  out->keyframes = ps::PtrVecClone(src.keyframes);
  return out;
}

bool operator==(const Model& a, const Model& b) {
  return a.model == b.model &&
         ps::PtrVecEq(a.compilers, b.compilers) &&
         ps::PtrVecEq(a.options, b.options) &&
         ps::PtrVecEq(a.sizes, b.sizes) &&
         ps::PtrVecEq(a.visuals, b.visuals) &&
         ps::PtrVecEq(a.statistics, b.statistics) &&
         ps::PtrVecEq(a.defaults, b.defaults) &&
         ps::PtrVecEq(a.extensions, b.extensions) &&
         ps::PtrVecEq(a.customs, b.customs) &&
         ps::PtrVecEq(a.assets, b.assets) &&
         ps::PtrVecEq(a.worldbody, b.worldbody) &&
         ps::PtrVecEq(a.deformables, b.deformables) &&
         ps::PtrVecEq(a.contacts, b.contacts) &&
         ps::PtrVecEq(a.equalitys, b.equalitys) &&
         ps::PtrVecEq(a.tendons, b.tendons) &&
         ps::PtrVecEq(a.actuators, b.actuators) &&
         ps::PtrVecEq(a.sensors, b.sensors) &&
         ps::PtrVecEq(a.keyframes, b.keyframes);
}

std::unique_ptr<ModelAsset> Clone(const ModelAsset& src) {
  auto out = std::make_unique<ModelAsset>();
  out->loc = src.loc;
  out->name = src.name;
  out->file = src.file;
  out->content_type = src.content_type;
  return out;
}

bool operator==(const ModelAsset& a, const ModelAsset& b) {
  return a.name == b.name &&
         a.file == b.file &&
         a.content_type == b.content_type;
}

std::unique_ptr<Motor> Clone(const Motor& src) {
  auto out = std::make_unique<Motor>();
  out->loc = src.loc;
  out->name = src.name;
  out->dclass = src.dclass;
  out->group = src.group;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->ctrllimited = src.ctrllimited;
  out->forcelimited = src.forcelimited;
  out->ctrlrange = src.ctrlrange;
  out->forcerange = src.forcerange;
  out->lengthrange = src.lengthrange;
  out->gear = src.gear;
  out->damping = src.damping;
  out->armature = src.armature;
  out->cranklength = src.cranklength;
  out->user = src.user;
  out->joint = src.joint;
  out->jointinparent = src.jointinparent;
  out->tendon = src.tendon;
  out->slidersite = src.slidersite;
  out->cranksite = src.cranksite;
  out->site = src.site;
  out->refsite = src.refsite;
  return out;
}

bool operator==(const Motor& a, const Motor& b) {
  return a.name == b.name &&
         a.dclass == b.dclass &&
         a.group == b.group &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.ctrllimited == b.ctrllimited &&
         a.forcelimited == b.forcelimited &&
         a.ctrlrange == b.ctrlrange &&
         a.forcerange == b.forcerange &&
         a.lengthrange == b.lengthrange &&
         a.gear == b.gear &&
         a.damping == b.damping &&
         a.armature == b.armature &&
         a.cranklength == b.cranklength &&
         a.user == b.user &&
         a.joint == b.joint &&
         a.jointinparent == b.jointinparent &&
         a.tendon == b.tendon &&
         a.slidersite == b.slidersite &&
         a.cranksite == b.cranksite &&
         a.site == b.site &&
         a.refsite == b.refsite;
}

std::unique_ptr<Muscle> Clone(const Muscle& src) {
  auto out = std::make_unique<Muscle>();
  out->loc = src.loc;
  out->name = src.name;
  out->dclass = src.dclass;
  out->group = src.group;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->ctrllimited = src.ctrllimited;
  out->forcelimited = src.forcelimited;
  out->ctrlrange = src.ctrlrange;
  out->forcerange = src.forcerange;
  out->lengthrange = src.lengthrange;
  out->gear = src.gear;
  out->damping = src.damping;
  out->armature = src.armature;
  out->cranklength = src.cranklength;
  out->user = src.user;
  out->joint = src.joint;
  out->jointinparent = src.jointinparent;
  out->tendon = src.tendon;
  out->slidersite = src.slidersite;
  out->cranksite = src.cranksite;
  out->timeconst = src.timeconst;
  out->tausmooth = src.tausmooth;
  out->range = src.range;
  out->force = src.force;
  out->scale = src.scale;
  out->lmin = src.lmin;
  out->lmax = src.lmax;
  out->vmax = src.vmax;
  out->fpmax = src.fpmax;
  out->fvmax = src.fvmax;
  return out;
}

bool operator==(const Muscle& a, const Muscle& b) {
  return a.name == b.name &&
         a.dclass == b.dclass &&
         a.group == b.group &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.ctrllimited == b.ctrllimited &&
         a.forcelimited == b.forcelimited &&
         a.ctrlrange == b.ctrlrange &&
         a.forcerange == b.forcerange &&
         a.lengthrange == b.lengthrange &&
         a.gear == b.gear &&
         a.damping == b.damping &&
         a.armature == b.armature &&
         a.cranklength == b.cranklength &&
         a.user == b.user &&
         a.joint == b.joint &&
         a.jointinparent == b.jointinparent &&
         a.tendon == b.tendon &&
         a.slidersite == b.slidersite &&
         a.cranksite == b.cranksite &&
         a.timeconst == b.timeconst &&
         a.tausmooth == b.tausmooth &&
         a.range == b.range &&
         a.force == b.force &&
         a.scale == b.scale &&
         a.lmin == b.lmin &&
         a.lmax == b.lmax &&
         a.vmax == b.vmax &&
         a.fpmax == b.fpmax &&
         a.fvmax == b.fvmax;
}

std::unique_ptr<Normal> Clone(const Normal& src) {
  auto out = std::make_unique<Normal>();
  out->loc = src.loc;
  out->name = src.name;
  out->geom1 = src.geom1;
  out->geom2 = src.geom2;
  out->body1 = src.body1;
  out->body2 = src.body2;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Normal& a, const Normal& b) {
  return a.name == b.name &&
         a.geom1 == b.geom1 &&
         a.geom2 == b.geom2 &&
         a.body1 == b.body1 &&
         a.body2 == b.body2 &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Numeric> Clone(const Numeric& src) {
  auto out = std::make_unique<Numeric>();
  out->loc = src.loc;
  out->name = src.name;
  out->data = src.data;
  return out;
}

bool operator==(const Numeric& a, const Numeric& b) {
  return a.name == b.name &&
         a.data == b.data;
}

std::unique_ptr<Option> Clone(const Option& src) {
  auto out = std::make_unique<Option>();
  out->loc = src.loc;
  out->timestep = src.timestep;
  out->impratio = src.impratio;
  out->tolerance = src.tolerance;
  out->ls_tolerance = src.ls_tolerance;
  out->noslip_tolerance = src.noslip_tolerance;
  out->ccd_tolerance = src.ccd_tolerance;
  out->sleep_tolerance = src.sleep_tolerance;
  out->gravity = src.gravity;
  out->wind = src.wind;
  out->magnetic = src.magnetic;
  out->density = src.density;
  out->viscosity = src.viscosity;
  out->o_margin = src.o_margin;
  out->o_solref = src.o_solref;
  out->o_solimp = src.o_solimp;
  out->o_friction = src.o_friction;
  out->integrator = src.integrator;
  out->cone = src.cone;
  out->jacobian = src.jacobian;
  out->solver = src.solver;
  out->iterations = src.iterations;
  out->ls_iterations = src.ls_iterations;
  out->noslip_iterations = src.noslip_iterations;
  out->ccd_iterations = src.ccd_iterations;
  out->sdf_iterations = src.sdf_iterations;
  out->sdf_initpoints = src.sdf_initpoints;
  out->actuatorgroupdisable = src.actuatorgroupdisable;
  out->flags = ps::PtrVecClone(src.flags);
  return out;
}

bool operator==(const Option& a, const Option& b) {
  return a.timestep == b.timestep &&
         a.impratio == b.impratio &&
         a.tolerance == b.tolerance &&
         a.ls_tolerance == b.ls_tolerance &&
         a.noslip_tolerance == b.noslip_tolerance &&
         a.ccd_tolerance == b.ccd_tolerance &&
         a.sleep_tolerance == b.sleep_tolerance &&
         a.gravity == b.gravity &&
         a.wind == b.wind &&
         a.magnetic == b.magnetic &&
         a.density == b.density &&
         a.viscosity == b.viscosity &&
         a.o_margin == b.o_margin &&
         a.o_solref == b.o_solref &&
         a.o_solimp == b.o_solimp &&
         a.o_friction == b.o_friction &&
         a.integrator == b.integrator &&
         a.cone == b.cone &&
         a.jacobian == b.jacobian &&
         a.solver == b.solver &&
         a.iterations == b.iterations &&
         a.ls_iterations == b.ls_iterations &&
         a.noslip_iterations == b.noslip_iterations &&
         a.ccd_iterations == b.ccd_iterations &&
         a.sdf_iterations == b.sdf_iterations &&
         a.sdf_initpoints == b.sdf_initpoints &&
         a.actuatorgroupdisable == b.actuatorgroupdisable &&
         ps::PtrVecEq(a.flags, b.flags);
}

std::unique_ptr<Pair> Clone(const Pair& src) {
  auto out = std::make_unique<Pair>();
  out->loc = src.loc;
  out->name = src.name;
  out->dclass = src.dclass;
  out->geom1 = src.geom1;
  out->geom2 = src.geom2;
  out->condim = src.condim;
  out->friction = src.friction;
  out->solref = src.solref;
  out->solreffriction = src.solreffriction;
  out->solimp = src.solimp;
  out->gap = src.gap;
  out->margin = src.margin;
  return out;
}

bool operator==(const Pair& a, const Pair& b) {
  return a.name == b.name &&
         a.dclass == b.dclass &&
         a.geom1 == b.geom1 &&
         a.geom2 == b.geom2 &&
         a.condim == b.condim &&
         a.friction == b.friction &&
         a.solref == b.solref &&
         a.solreffriction == b.solreffriction &&
         a.solimp == b.solimp &&
         a.gap == b.gap &&
         a.margin == b.margin;
}

std::unique_ptr<PluginDef> Clone(const PluginDef& src) {
  auto out = std::make_unique<PluginDef>();
  out->loc = src.loc;
  out->plugin = src.plugin;
  out->pluginInstances = ps::PtrVecClone(src.pluginInstances);
  return out;
}

bool operator==(const PluginDef& a, const PluginDef& b) {
  return a.plugin == b.plugin &&
         ps::PtrVecEq(a.pluginInstances, b.pluginInstances);
}

std::unique_ptr<PluginInstance> Clone(const PluginInstance& src) {
  auto out = std::make_unique<PluginInstance>();
  out->loc = src.loc;
  out->name = src.name;
  out->config = ps::PtrVecClone(src.config);
  return out;
}

bool operator==(const PluginInstance& a, const PluginInstance& b) {
  return a.name == b.name &&
         ps::PtrVecEq(a.config, b.config);
}

std::unique_ptr<PluginRef> Clone(const PluginRef& src) {
  auto out = std::make_unique<PluginRef>();
  out->loc = src.loc;
  out->plugin = src.plugin;
  out->instance = src.instance;
  out->config = ps::PtrVecClone(src.config);
  return out;
}

bool operator==(const PluginRef& a, const PluginRef& b) {
  return a.plugin == b.plugin &&
         a.instance == b.instance &&
         ps::PtrVecEq(a.config, b.config);
}

std::unique_ptr<Position> Clone(const Position& src) {
  auto out = std::make_unique<Position>();
  out->loc = src.loc;
  out->name = src.name;
  out->dclass = src.dclass;
  out->group = src.group;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->ctrllimited = src.ctrllimited;
  out->forcelimited = src.forcelimited;
  out->ctrlrange = src.ctrlrange;
  out->inheritrange = src.inheritrange;
  out->forcerange = src.forcerange;
  out->lengthrange = src.lengthrange;
  out->gear = src.gear;
  out->damping = src.damping;
  out->armature = src.armature;
  out->cranklength = src.cranklength;
  out->user = src.user;
  out->joint = src.joint;
  out->jointinparent = src.jointinparent;
  out->tendon = src.tendon;
  out->slidersite = src.slidersite;
  out->cranksite = src.cranksite;
  out->site = src.site;
  out->refsite = src.refsite;
  out->kp = src.kp;
  out->kv = src.kv;
  out->dampratio = src.dampratio;
  out->timeconst = src.timeconst;
  return out;
}

bool operator==(const Position& a, const Position& b) {
  return a.name == b.name &&
         a.dclass == b.dclass &&
         a.group == b.group &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.ctrllimited == b.ctrllimited &&
         a.forcelimited == b.forcelimited &&
         a.ctrlrange == b.ctrlrange &&
         a.inheritrange == b.inheritrange &&
         a.forcerange == b.forcerange &&
         a.lengthrange == b.lengthrange &&
         a.gear == b.gear &&
         a.damping == b.damping &&
         a.armature == b.armature &&
         a.cranklength == b.cranklength &&
         a.user == b.user &&
         a.joint == b.joint &&
         a.jointinparent == b.jointinparent &&
         a.tendon == b.tendon &&
         a.slidersite == b.slidersite &&
         a.cranksite == b.cranksite &&
         a.site == b.site &&
         a.refsite == b.refsite &&
         a.kp == b.kp &&
         a.kv == b.kv &&
         a.dampratio == b.dampratio &&
         a.timeconst == b.timeconst;
}

std::unique_ptr<Pulley> Clone(const Pulley& src) {
  auto out = std::make_unique<Pulley>();
  out->loc = src.loc;
  out->divisor = src.divisor;
  return out;
}

bool operator==(const Pulley& a, const Pulley& b) {
  return a.divisor == b.divisor;
}

std::unique_ptr<Rangefinder> Clone(const Rangefinder& src) {
  auto out = std::make_unique<Rangefinder>();
  out->loc = src.loc;
  out->name = src.name;
  out->site = src.site;
  out->camera = src.camera;
  out->data = src.data;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Rangefinder& a, const Rangefinder& b) {
  return a.name == b.name &&
         a.site == b.site &&
         a.camera == b.camera &&
         a.data == b.data &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Replicate> Clone(const Replicate& src) {
  auto out = std::make_unique<Replicate>();
  out->loc = src.loc;
  out->count = src.count;
  out->offset = src.offset;
  out->euler = src.euler;
  out->sep = src.sep;
  out->prefix = src.prefix;
  out->childclass = src.childclass;
  out->inertial = ps::PtrVecClone(src.inertial);
  for (const auto& item : src.subtree)
    out->subtree.push_back(Clone(item));
  return out;
}

bool operator==(const Replicate& a, const Replicate& b) {
  return a.count == b.count &&
         a.offset == b.offset &&
         a.euler == b.euler &&
         a.sep == b.sep &&
         a.prefix == b.prefix &&
         a.childclass == b.childclass &&
         ps::PtrVecEq(a.inertial, b.inertial) &&
         a.subtree == b.subtree;
}

std::unique_ptr<Sensor> Clone(const Sensor& src) {
  auto out = std::make_unique<Sensor>();
  out->loc = src.loc;
  for (const auto& item : src.sensors)
    out->sensors.push_back(Clone(item));
  return out;
}

bool operator==(const Sensor& a, const Sensor& b) {
  return a.sensors == b.sensors;
}

std::unique_ptr<SensorContact> Clone(const SensorContact& src) {
  auto out = std::make_unique<SensorContact>();
  out->loc = src.loc;
  out->name = src.name;
  out->geom1 = src.geom1;
  out->geom2 = src.geom2;
  out->body1 = src.body1;
  out->body2 = src.body2;
  out->subtree1 = src.subtree1;
  out->subtree2 = src.subtree2;
  out->site = src.site;
  out->num = src.num;
  out->data = src.data;
  out->reduce = src.reduce;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const SensorContact& a, const SensorContact& b) {
  return a.name == b.name &&
         a.geom1 == b.geom1 &&
         a.geom2 == b.geom2 &&
         a.body1 == b.body1 &&
         a.body2 == b.body2 &&
         a.subtree1 == b.subtree1 &&
         a.subtree2 == b.subtree2 &&
         a.site == b.site &&
         a.num == b.num &&
         a.data == b.data &&
         a.reduce == b.reduce &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<SensorPlugin> Clone(const SensorPlugin& src) {
  auto out = std::make_unique<SensorPlugin>();
  out->loc = src.loc;
  out->name = src.name;
  out->plugin = src.plugin;
  out->instance = src.instance;
  out->cutoff = src.cutoff;
  out->objtype = src.objtype;
  out->objname = src.objname;
  out->reftype = src.reftype;
  out->refname = src.refname;
  out->user = src.user;
  out->config = ps::PtrVecClone(src.config);
  return out;
}

bool operator==(const SensorPlugin& a, const SensorPlugin& b) {
  return a.name == b.name &&
         a.plugin == b.plugin &&
         a.instance == b.instance &&
         a.cutoff == b.cutoff &&
         a.objtype == b.objtype &&
         a.objname == b.objname &&
         a.reftype == b.reftype &&
         a.refname == b.refname &&
         a.user == b.user &&
         ps::PtrVecEq(a.config, b.config);
}

std::unique_ptr<SensorUser> Clone(const SensorUser& src) {
  auto out = std::make_unique<SensorUser>();
  out->loc = src.loc;
  out->name = src.name;
  out->objtype = src.objtype;
  out->objname = src.objname;
  out->datatype = src.datatype;
  out->needstage = src.needstage;
  out->dim = src.dim;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const SensorUser& a, const SensorUser& b) {
  return a.name == b.name &&
         a.objtype == b.objtype &&
         a.objname == b.objname &&
         a.datatype == b.datatype &&
         a.needstage == b.needstage &&
         a.dim == b.dim &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Site> Clone(const Site& src) {
  auto out = std::make_unique<Site>();
  out->loc = src.loc;
  out->pos = src.pos;
  out->quat = src.quat;
  out->name = src.name;
  out->dclass = src.dclass;
  out->type = src.type;
  out->group = src.group;
  out->material = src.material;
  out->size = src.size;
  out->rgba = src.rgba;
  out->user = src.user;
  out->shape = src.shape;
  return out;
}

bool operator==(const Site& a, const Site& b) {
  return a.pos == b.pos &&
         a.quat == b.quat &&
         a.name == b.name &&
         a.dclass == b.dclass &&
         a.type == b.type &&
         a.group == b.group &&
         a.material == b.material &&
         a.size == b.size &&
         a.rgba == b.rgba &&
         a.user == b.user &&
         a.shape == b.shape;
}

std::unique_ptr<Size> Clone(const Size& src) {
  auto out = std::make_unique<Size>();
  out->loc = src.loc;
  out->memory = src.memory;
  out->njmax = src.njmax;
  out->nconmax = src.nconmax;
  out->nstack = src.nstack;
  out->nuserdata = src.nuserdata;
  out->nkey = src.nkey;
  out->nuser_body = src.nuser_body;
  out->nuser_jnt = src.nuser_jnt;
  out->nuser_geom = src.nuser_geom;
  out->nuser_site = src.nuser_site;
  out->nuser_cam = src.nuser_cam;
  out->nuser_tendon = src.nuser_tendon;
  out->nuser_actuator = src.nuser_actuator;
  out->nuser_sensor = src.nuser_sensor;
  return out;
}

bool operator==(const Size& a, const Size& b) {
  return a.memory == b.memory &&
         a.njmax == b.njmax &&
         a.nconmax == b.nconmax &&
         a.nstack == b.nstack &&
         a.nuserdata == b.nuserdata &&
         a.nkey == b.nkey &&
         a.nuser_body == b.nuser_body &&
         a.nuser_jnt == b.nuser_jnt &&
         a.nuser_geom == b.nuser_geom &&
         a.nuser_site == b.nuser_site &&
         a.nuser_cam == b.nuser_cam &&
         a.nuser_tendon == b.nuser_tendon &&
         a.nuser_actuator == b.nuser_actuator &&
         a.nuser_sensor == b.nuser_sensor;
}

std::unique_ptr<Skin> Clone(const Skin& src) {
  auto out = std::make_unique<Skin>();
  out->loc = src.loc;
  out->name = src.name;
  out->file = src.file;
  out->material = src.material;
  out->rgba = src.rgba;
  out->inflate = src.inflate;
  out->vertex = src.vertex;
  out->texcoord = src.texcoord;
  out->face = src.face;
  out->group = src.group;
  out->bones = ps::PtrVecClone(src.bones);
  return out;
}

bool operator==(const Skin& a, const Skin& b) {
  return a.name == b.name &&
         a.file == b.file &&
         a.material == b.material &&
         a.rgba == b.rgba &&
         a.inflate == b.inflate &&
         a.vertex == b.vertex &&
         a.texcoord == b.texcoord &&
         a.face == b.face &&
         a.group == b.group &&
         ps::PtrVecEq(a.bones, b.bones);
}

std::unique_ptr<SkinBone> Clone(const SkinBone& src) {
  auto out = std::make_unique<SkinBone>();
  out->loc = src.loc;
  out->body = src.body;
  out->bindpos = src.bindpos;
  out->bindquat = src.bindquat;
  out->vertid = src.vertid;
  out->vertweight = src.vertweight;
  return out;
}

bool operator==(const SkinBone& a, const SkinBone& b) {
  return a.body == b.body &&
         a.bindpos == b.bindpos &&
         a.bindquat == b.bindquat &&
         a.vertid == b.vertid &&
         a.vertweight == b.vertweight;
}

std::unique_ptr<Spatial> Clone(const Spatial& src) {
  auto out = std::make_unique<Spatial>();
  out->loc = src.loc;
  out->name = src.name;
  out->dclass = src.dclass;
  out->group = src.group;
  out->limited = src.limited;
  out->actuatorfrclimited = src.actuatorfrclimited;
  out->range = src.range;
  out->actuatorfrcrange = src.actuatorfrcrange;
  out->solreflimit = src.solreflimit;
  out->solimplimit = src.solimplimit;
  out->solreffriction = src.solreffriction;
  out->solimpfriction = src.solimpfriction;
  out->frictionloss = src.frictionloss;
  out->springlength = src.springlength;
  out->width = src.width;
  out->material = src.material;
  out->margin = src.margin;
  out->stiffness = src.stiffness;
  out->damping = src.damping;
  out->armature = src.armature;
  out->rgba = src.rgba;
  out->user = src.user;
  for (const auto& item : src.path)
    out->path.push_back(Clone(item));
  return out;
}

bool operator==(const Spatial& a, const Spatial& b) {
  return a.name == b.name &&
         a.dclass == b.dclass &&
         a.group == b.group &&
         a.limited == b.limited &&
         a.actuatorfrclimited == b.actuatorfrclimited &&
         a.range == b.range &&
         a.actuatorfrcrange == b.actuatorfrcrange &&
         a.solreflimit == b.solreflimit &&
         a.solimplimit == b.solimplimit &&
         a.solreffriction == b.solreffriction &&
         a.solimpfriction == b.solimpfriction &&
         a.frictionloss == b.frictionloss &&
         a.springlength == b.springlength &&
         a.width == b.width &&
         a.material == b.material &&
         a.margin == b.margin &&
         a.stiffness == b.stiffness &&
         a.damping == b.damping &&
         a.armature == b.armature &&
         a.rgba == b.rgba &&
         a.user == b.user &&
         a.path == b.path;
}

std::unique_ptr<SpatialGeom> Clone(const SpatialGeom& src) {
  auto out = std::make_unique<SpatialGeom>();
  out->loc = src.loc;
  out->geom = src.geom;
  out->sidesite = src.sidesite;
  return out;
}

bool operator==(const SpatialGeom& a, const SpatialGeom& b) {
  return a.geom == b.geom &&
         a.sidesite == b.sidesite;
}

std::unique_ptr<SpatialSite> Clone(const SpatialSite& src) {
  auto out = std::make_unique<SpatialSite>();
  out->loc = src.loc;
  out->site = src.site;
  return out;
}

bool operator==(const SpatialSite& a, const SpatialSite& b) {
  return a.site == b.site;
}

std::unique_ptr<Statistic> Clone(const Statistic& src) {
  auto out = std::make_unique<Statistic>();
  out->loc = src.loc;
  out->meaninertia = src.meaninertia;
  out->meanmass = src.meanmass;
  out->meansize = src.meansize;
  out->extent = src.extent;
  out->center = src.center;
  return out;
}

bool operator==(const Statistic& a, const Statistic& b) {
  return a.meaninertia == b.meaninertia &&
         a.meanmass == b.meanmass &&
         a.meansize == b.meansize &&
         a.extent == b.extent &&
         a.center == b.center;
}

std::unique_ptr<Subtreeangmom> Clone(const Subtreeangmom& src) {
  auto out = std::make_unique<Subtreeangmom>();
  out->loc = src.loc;
  out->name = src.name;
  out->body = src.body;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Subtreeangmom& a, const Subtreeangmom& b) {
  return a.name == b.name &&
         a.body == b.body &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Subtreecom> Clone(const Subtreecom& src) {
  auto out = std::make_unique<Subtreecom>();
  out->loc = src.loc;
  out->name = src.name;
  out->body = src.body;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Subtreecom& a, const Subtreecom& b) {
  return a.name == b.name &&
         a.body == b.body &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Subtreelinvel> Clone(const Subtreelinvel& src) {
  auto out = std::make_unique<Subtreelinvel>();
  out->loc = src.loc;
  out->name = src.name;
  out->body = src.body;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Subtreelinvel& a, const Subtreelinvel& b) {
  return a.name == b.name &&
         a.body == b.body &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Tactile> Clone(const Tactile& src) {
  auto out = std::make_unique<Tactile>();
  out->loc = src.loc;
  out->name = src.name;
  out->geom = src.geom;
  out->mesh = src.mesh;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->user = src.user;
  return out;
}

bool operator==(const Tactile& a, const Tactile& b) {
  return a.name == b.name &&
         a.geom == b.geom &&
         a.mesh == b.mesh &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.user == b.user;
}

std::unique_ptr<Tendon> Clone(const Tendon& src) {
  auto out = std::make_unique<Tendon>();
  out->loc = src.loc;
  for (const auto& item : src.tendons)
    out->tendons.push_back(Clone(item));
  return out;
}

bool operator==(const Tendon& a, const Tendon& b) {
  return a.tendons == b.tendons;
}

std::unique_ptr<TendonDefault> Clone(const TendonDefault& src) {
  auto out = std::make_unique<TendonDefault>();
  out->loc = src.loc;
  out->group = src.group;
  out->limited = src.limited;
  out->range = src.range;
  out->solreflimit = src.solreflimit;
  out->solimplimit = src.solimplimit;
  out->solreffriction = src.solreffriction;
  out->solimpfriction = src.solimpfriction;
  out->frictionloss = src.frictionloss;
  out->springlength = src.springlength;
  out->width = src.width;
  out->material = src.material;
  out->margin = src.margin;
  out->stiffness = src.stiffness;
  out->damping = src.damping;
  out->rgba = src.rgba;
  out->user = src.user;
  return out;
}

bool operator==(const TendonDefault& a, const TendonDefault& b) {
  return a.group == b.group &&
         a.limited == b.limited &&
         a.range == b.range &&
         a.solreflimit == b.solreflimit &&
         a.solimplimit == b.solimplimit &&
         a.solreffriction == b.solreffriction &&
         a.solimpfriction == b.solimpfriction &&
         a.frictionloss == b.frictionloss &&
         a.springlength == b.springlength &&
         a.width == b.width &&
         a.material == b.material &&
         a.margin == b.margin &&
         a.stiffness == b.stiffness &&
         a.damping == b.damping &&
         a.rgba == b.rgba &&
         a.user == b.user;
}

std::unique_ptr<Tendonactuatorfrc> Clone(const Tendonactuatorfrc& src) {
  auto out = std::make_unique<Tendonactuatorfrc>();
  out->loc = src.loc;
  out->name = src.name;
  out->tendon = src.tendon;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Tendonactuatorfrc& a, const Tendonactuatorfrc& b) {
  return a.name == b.name &&
         a.tendon == b.tendon &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Tendonlimitfrc> Clone(const Tendonlimitfrc& src) {
  auto out = std::make_unique<Tendonlimitfrc>();
  out->loc = src.loc;
  out->name = src.name;
  out->tendon = src.tendon;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Tendonlimitfrc& a, const Tendonlimitfrc& b) {
  return a.name == b.name &&
         a.tendon == b.tendon &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Tendonlimitpos> Clone(const Tendonlimitpos& src) {
  auto out = std::make_unique<Tendonlimitpos>();
  out->loc = src.loc;
  out->name = src.name;
  out->tendon = src.tendon;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Tendonlimitpos& a, const Tendonlimitpos& b) {
  return a.name == b.name &&
         a.tendon == b.tendon &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Tendonlimitvel> Clone(const Tendonlimitvel& src) {
  auto out = std::make_unique<Tendonlimitvel>();
  out->loc = src.loc;
  out->name = src.name;
  out->tendon = src.tendon;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Tendonlimitvel& a, const Tendonlimitvel& b) {
  return a.name == b.name &&
         a.tendon == b.tendon &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Tendonpos> Clone(const Tendonpos& src) {
  auto out = std::make_unique<Tendonpos>();
  out->loc = src.loc;
  out->name = src.name;
  out->tendon = src.tendon;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Tendonpos& a, const Tendonpos& b) {
  return a.name == b.name &&
         a.tendon == b.tendon &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Tendonvel> Clone(const Tendonvel& src) {
  auto out = std::make_unique<Tendonvel>();
  out->loc = src.loc;
  out->name = src.name;
  out->tendon = src.tendon;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Tendonvel& a, const Tendonvel& b) {
  return a.name == b.name &&
         a.tendon == b.tendon &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Text> Clone(const Text& src) {
  auto out = std::make_unique<Text>();
  out->loc = src.loc;
  out->name = src.name;
  out->data = src.data;
  return out;
}

bool operator==(const Text& a, const Text& b) {
  return a.name == b.name &&
         a.data == b.data;
}

std::unique_ptr<Texture> Clone(const Texture& src) {
  auto out = std::make_unique<Texture>();
  out->loc = src.loc;
  out->name = src.name;
  out->type = src.type;
  out->colorspace = src.colorspace;
  out->content_type = src.content_type;
  out->gridsize = src.gridsize;
  out->gridlayout = src.gridlayout;
  out->fileright = src.fileright;
  out->fileleft = src.fileleft;
  out->fileup = src.fileup;
  out->filedown = src.filedown;
  out->filefront = src.filefront;
  out->fileback = src.fileback;
  out->rgb1 = src.rgb1;
  out->rgb2 = src.rgb2;
  out->mark = src.mark;
  out->markrgb = src.markrgb;
  out->random = src.random;
  out->width = src.width;
  out->height = src.height;
  out->hflip = src.hflip;
  out->vflip = src.vflip;
  out->nchannel = src.nchannel;
  out->source = src.source;
  return out;
}

bool operator==(const Texture& a, const Texture& b) {
  return a.name == b.name &&
         a.type == b.type &&
         a.colorspace == b.colorspace &&
         a.content_type == b.content_type &&
         a.gridsize == b.gridsize &&
         a.gridlayout == b.gridlayout &&
         a.fileright == b.fileright &&
         a.fileleft == b.fileleft &&
         a.fileup == b.fileup &&
         a.filedown == b.filedown &&
         a.filefront == b.filefront &&
         a.fileback == b.fileback &&
         a.rgb1 == b.rgb1 &&
         a.rgb2 == b.rgb2 &&
         a.mark == b.mark &&
         a.markrgb == b.markrgb &&
         a.random == b.random &&
         a.width == b.width &&
         a.height == b.height &&
         a.hflip == b.hflip &&
         a.vflip == b.vflip &&
         a.nchannel == b.nchannel &&
         a.source == b.source;
}

std::unique_ptr<Torque> Clone(const Torque& src) {
  auto out = std::make_unique<Torque>();
  out->loc = src.loc;
  out->name = src.name;
  out->site = src.site;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Torque& a, const Torque& b) {
  return a.name == b.name &&
         a.site == b.site &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Touch> Clone(const Touch& src) {
  auto out = std::make_unique<Touch>();
  out->loc = src.loc;
  out->name = src.name;
  out->site = src.site;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Touch& a, const Touch& b) {
  return a.name == b.name &&
         a.site == b.site &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Tuple> Clone(const Tuple& src) {
  auto out = std::make_unique<Tuple>();
  out->loc = src.loc;
  out->name = src.name;
  out->tupleElements = ps::PtrVecClone(src.tupleElements);
  return out;
}

bool operator==(const Tuple& a, const Tuple& b) {
  return a.name == b.name &&
         ps::PtrVecEq(a.tupleElements, b.tupleElements);
}

std::unique_ptr<TupleElement> Clone(const TupleElement& src) {
  auto out = std::make_unique<TupleElement>();
  out->loc = src.loc;
  out->objtype = src.objtype;
  out->objname = src.objname;
  out->prm = src.prm;
  return out;
}

bool operator==(const TupleElement& a, const TupleElement& b) {
  return a.objtype == b.objtype &&
         a.objname == b.objname &&
         a.prm == b.prm;
}

std::unique_ptr<Velocimeter> Clone(const Velocimeter& src) {
  auto out = std::make_unique<Velocimeter>();
  out->loc = src.loc;
  out->name = src.name;
  out->site = src.site;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->interval = src.interval;
  out->cutoff = src.cutoff;
  out->noise = src.noise;
  out->user = src.user;
  return out;
}

bool operator==(const Velocimeter& a, const Velocimeter& b) {
  return a.name == b.name &&
         a.site == b.site &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.interval == b.interval &&
         a.cutoff == b.cutoff &&
         a.noise == b.noise &&
         a.user == b.user;
}

std::unique_ptr<Velocity> Clone(const Velocity& src) {
  auto out = std::make_unique<Velocity>();
  out->loc = src.loc;
  out->name = src.name;
  out->dclass = src.dclass;
  out->group = src.group;
  out->nsample = src.nsample;
  out->interp = src.interp;
  out->delay = src.delay;
  out->ctrllimited = src.ctrllimited;
  out->forcelimited = src.forcelimited;
  out->ctrlrange = src.ctrlrange;
  out->forcerange = src.forcerange;
  out->lengthrange = src.lengthrange;
  out->gear = src.gear;
  out->damping = src.damping;
  out->armature = src.armature;
  out->cranklength = src.cranklength;
  out->user = src.user;
  out->joint = src.joint;
  out->jointinparent = src.jointinparent;
  out->tendon = src.tendon;
  out->slidersite = src.slidersite;
  out->cranksite = src.cranksite;
  out->site = src.site;
  out->refsite = src.refsite;
  out->kv = src.kv;
  return out;
}

bool operator==(const Velocity& a, const Velocity& b) {
  return a.name == b.name &&
         a.dclass == b.dclass &&
         a.group == b.group &&
         a.nsample == b.nsample &&
         a.interp == b.interp &&
         a.delay == b.delay &&
         a.ctrllimited == b.ctrllimited &&
         a.forcelimited == b.forcelimited &&
         a.ctrlrange == b.ctrlrange &&
         a.forcerange == b.forcerange &&
         a.lengthrange == b.lengthrange &&
         a.gear == b.gear &&
         a.damping == b.damping &&
         a.armature == b.armature &&
         a.cranklength == b.cranklength &&
         a.user == b.user &&
         a.joint == b.joint &&
         a.jointinparent == b.jointinparent &&
         a.tendon == b.tendon &&
         a.slidersite == b.slidersite &&
         a.cranksite == b.cranksite &&
         a.site == b.site &&
         a.refsite == b.refsite &&
         a.kv == b.kv;
}

std::unique_ptr<Visual> Clone(const Visual& src) {
  auto out = std::make_unique<Visual>();
  out->loc = src.loc;
  out->visualGlobals = ps::PtrVecClone(src.visualGlobals);
  out->visualQualitys = ps::PtrVecClone(src.visualQualitys);
  out->visualHeadlights = ps::PtrVecClone(src.visualHeadlights);
  out->visualMaps = ps::PtrVecClone(src.visualMaps);
  out->visualScales = ps::PtrVecClone(src.visualScales);
  out->visualRgbas = ps::PtrVecClone(src.visualRgbas);
  return out;
}

bool operator==(const Visual& a, const Visual& b) {
  return ps::PtrVecEq(a.visualGlobals, b.visualGlobals) &&
         ps::PtrVecEq(a.visualQualitys, b.visualQualitys) &&
         ps::PtrVecEq(a.visualHeadlights, b.visualHeadlights) &&
         ps::PtrVecEq(a.visualMaps, b.visualMaps) &&
         ps::PtrVecEq(a.visualScales, b.visualScales) &&
         ps::PtrVecEq(a.visualRgbas, b.visualRgbas);
}

std::unique_ptr<VisualGlobal> Clone(const VisualGlobal& src) {
  auto out = std::make_unique<VisualGlobal>();
  out->loc = src.loc;
  out->cameraid = src.cameraid;
  out->orthographic = src.orthographic;
  out->fovy = src.fovy;
  out->ipd = src.ipd;
  out->azimuth = src.azimuth;
  out->elevation = src.elevation;
  out->linewidth = src.linewidth;
  out->glow = src.glow;
  out->offwidth = src.offwidth;
  out->offheight = src.offheight;
  out->realtime = src.realtime;
  out->ellipsoidinertia = src.ellipsoidinertia;
  out->bvactive = src.bvactive;
  return out;
}

bool operator==(const VisualGlobal& a, const VisualGlobal& b) {
  return a.cameraid == b.cameraid &&
         a.orthographic == b.orthographic &&
         a.fovy == b.fovy &&
         a.ipd == b.ipd &&
         a.azimuth == b.azimuth &&
         a.elevation == b.elevation &&
         a.linewidth == b.linewidth &&
         a.glow == b.glow &&
         a.offwidth == b.offwidth &&
         a.offheight == b.offheight &&
         a.realtime == b.realtime &&
         a.ellipsoidinertia == b.ellipsoidinertia &&
         a.bvactive == b.bvactive;
}

std::unique_ptr<VisualHeadlight> Clone(const VisualHeadlight& src) {
  auto out = std::make_unique<VisualHeadlight>();
  out->loc = src.loc;
  out->ambient = src.ambient;
  out->diffuse = src.diffuse;
  out->specular = src.specular;
  out->active = src.active;
  return out;
}

bool operator==(const VisualHeadlight& a, const VisualHeadlight& b) {
  return a.ambient == b.ambient &&
         a.diffuse == b.diffuse &&
         a.specular == b.specular &&
         a.active == b.active;
}

std::unique_ptr<VisualMap> Clone(const VisualMap& src) {
  auto out = std::make_unique<VisualMap>();
  out->loc = src.loc;
  out->stiffness = src.stiffness;
  out->stiffnessrot = src.stiffnessrot;
  out->force = src.force;
  out->torque = src.torque;
  out->alpha = src.alpha;
  out->fogstart = src.fogstart;
  out->fogend = src.fogend;
  out->znear = src.znear;
  out->zfar = src.zfar;
  out->haze = src.haze;
  out->shadowclip = src.shadowclip;
  out->shadowscale = src.shadowscale;
  out->actuatortendon = src.actuatortendon;
  return out;
}

bool operator==(const VisualMap& a, const VisualMap& b) {
  return a.stiffness == b.stiffness &&
         a.stiffnessrot == b.stiffnessrot &&
         a.force == b.force &&
         a.torque == b.torque &&
         a.alpha == b.alpha &&
         a.fogstart == b.fogstart &&
         a.fogend == b.fogend &&
         a.znear == b.znear &&
         a.zfar == b.zfar &&
         a.haze == b.haze &&
         a.shadowclip == b.shadowclip &&
         a.shadowscale == b.shadowscale &&
         a.actuatortendon == b.actuatortendon;
}

std::unique_ptr<VisualQuality> Clone(const VisualQuality& src) {
  auto out = std::make_unique<VisualQuality>();
  out->loc = src.loc;
  out->shadowsize = src.shadowsize;
  out->offsamples = src.offsamples;
  out->numslices = src.numslices;
  out->numstacks = src.numstacks;
  out->numquads = src.numquads;
  return out;
}

bool operator==(const VisualQuality& a, const VisualQuality& b) {
  return a.shadowsize == b.shadowsize &&
         a.offsamples == b.offsamples &&
         a.numslices == b.numslices &&
         a.numstacks == b.numstacks &&
         a.numquads == b.numquads;
}

std::unique_ptr<VisualRgba> Clone(const VisualRgba& src) {
  auto out = std::make_unique<VisualRgba>();
  out->loc = src.loc;
  out->fog = src.fog;
  out->haze = src.haze;
  out->force = src.force;
  out->inertia = src.inertia;
  out->joint = src.joint;
  out->actuator = src.actuator;
  out->actuatornegative = src.actuatornegative;
  out->actuatorpositive = src.actuatorpositive;
  out->com = src.com;
  out->camera = src.camera;
  out->light = src.light;
  out->selectpoint = src.selectpoint;
  out->connect = src.connect;
  out->contactpoint = src.contactpoint;
  out->contactforce = src.contactforce;
  out->contactfriction = src.contactfriction;
  out->contacttorque = src.contacttorque;
  out->contactgap = src.contactgap;
  out->rangefinder = src.rangefinder;
  out->constraint = src.constraint;
  out->slidercrank = src.slidercrank;
  out->crankbroken = src.crankbroken;
  out->frustum = src.frustum;
  out->bv = src.bv;
  out->bvactive = src.bvactive;
  return out;
}

bool operator==(const VisualRgba& a, const VisualRgba& b) {
  return a.fog == b.fog &&
         a.haze == b.haze &&
         a.force == b.force &&
         a.inertia == b.inertia &&
         a.joint == b.joint &&
         a.actuator == b.actuator &&
         a.actuatornegative == b.actuatornegative &&
         a.actuatorpositive == b.actuatorpositive &&
         a.com == b.com &&
         a.camera == b.camera &&
         a.light == b.light &&
         a.selectpoint == b.selectpoint &&
         a.connect == b.connect &&
         a.contactpoint == b.contactpoint &&
         a.contactforce == b.contactforce &&
         a.contactfriction == b.contactfriction &&
         a.contacttorque == b.contacttorque &&
         a.contactgap == b.contactgap &&
         a.rangefinder == b.rangefinder &&
         a.constraint == b.constraint &&
         a.slidercrank == b.slidercrank &&
         a.crankbroken == b.crankbroken &&
         a.frustum == b.frustum &&
         a.bv == b.bv &&
         a.bvactive == b.bvactive;
}

std::unique_ptr<VisualScale> Clone(const VisualScale& src) {
  auto out = std::make_unique<VisualScale>();
  out->loc = src.loc;
  out->forcewidth = src.forcewidth;
  out->contactwidth = src.contactwidth;
  out->contactheight = src.contactheight;
  out->connect = src.connect;
  out->com = src.com;
  out->camera = src.camera;
  out->light = src.light;
  out->selectpoint = src.selectpoint;
  out->jointlength = src.jointlength;
  out->jointwidth = src.jointwidth;
  out->actuatorlength = src.actuatorlength;
  out->actuatorwidth = src.actuatorwidth;
  out->framelength = src.framelength;
  out->framewidth = src.framewidth;
  out->constraint = src.constraint;
  out->slidercrank = src.slidercrank;
  out->frustum = src.frustum;
  return out;
}

bool operator==(const VisualScale& a, const VisualScale& b) {
  return a.forcewidth == b.forcewidth &&
         a.contactwidth == b.contactwidth &&
         a.contactheight == b.contactheight &&
         a.connect == b.connect &&
         a.com == b.com &&
         a.camera == b.camera &&
         a.light == b.light &&
         a.selectpoint == b.selectpoint &&
         a.jointlength == b.jointlength &&
         a.jointwidth == b.jointwidth &&
         a.actuatorlength == b.actuatorlength &&
         a.actuatorwidth == b.actuatorwidth &&
         a.framelength == b.framelength &&
         a.framewidth == b.framewidth &&
         a.constraint == b.constraint &&
         a.slidercrank == b.slidercrank &&
         a.frustum == b.frustum;
}

std::unique_ptr<Weld> Clone(const Weld& src) {
  auto out = std::make_unique<Weld>();
  out->loc = src.loc;
  out->name = src.name;
  out->dclass = src.dclass;
  out->body1 = src.body1;
  out->body2 = src.body2;
  out->relpose = src.relpose;
  out->anchor = src.anchor;
  out->site1 = src.site1;
  out->site2 = src.site2;
  out->active = src.active;
  out->solref = src.solref;
  out->solimp = src.solimp;
  out->torquescale = src.torquescale;
  return out;
}

bool operator==(const Weld& a, const Weld& b) {
  return a.name == b.name &&
         a.dclass == b.dclass &&
         a.body1 == b.body1 &&
         a.body2 == b.body2 &&
         a.relpose == b.relpose &&
         a.anchor == b.anchor &&
         a.site1 == b.site1 &&
         a.site2 == b.site2 &&
         a.active == b.active &&
         a.solref == b.solref &&
         a.solimp == b.solimp &&
         a.torquescale == b.torquescale;
}

}  // namespace ps::mjcf
